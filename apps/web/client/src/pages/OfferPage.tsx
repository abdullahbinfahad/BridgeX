import { useAuth } from "@/_core/hooks/useAuth";
import PublicLayout from "@/components/bridgex/PublicLayout";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Textarea } from "@/components/ui/textarea";
import { supabase } from "@/lib/supabase";
import { AlertTriangle, ArrowLeft, CalendarDays, CheckCircle2, CircleDollarSign, Plane, ShieldCheck } from "lucide-react";
import { useEffect, useState } from "react";
import { Link, useLocation } from "wouter";

type Request = { id: string; title: string; purchase_country: string; purchase_city: string | null; destination_country: string | null; destination_city: string; weight_kg: number; size_description: string | null; delivery_required_days: number | null; special_handling: string | null; budget_bdt: number; currency: string; status: string; user_id: string };

export default function OfferPage() {
  const { user, isAuthenticated } = useAuth();
  const [, setLocation] = useLocation();
  const [request, setRequest] = useState<Request | null>(null);
  const [loading, setLoading] = useState(true);
  const [amount, setAmount] = useState("");
  const [date, setDate] = useState("");
  const [note, setNote] = useState("");
  const [status, setStatus] = useState<"idle" | "sending" | "sent" | "error">("idle");
  const [message, setMessage] = useState("");
  const requestId = new URLSearchParams(window.location.search).get("request");

  useEffect(() => { if (!requestId) { setLoading(false); return; } void (async () => { const { data, error } = await supabase.from("send_requests").select("id,title,purchase_country,purchase_city,destination_country,destination_city,weight_kg,size_description,delivery_required_days,special_handling,budget_bdt,currency,status,user_id").eq("id", requestId).eq("status", "open").maybeSingle(); if (error) { setMessage(error.message); setStatus("error"); } setRequest(data as Request | null); setLoading(false); })(); }, [requestId]);

  const currency = request?.currency || "BDT";
  const submit = async (event: React.FormEvent) => {
    event.preventDefault();
    if (!isAuthenticated || !user) return setLocation("/access");
    if (!request) return setMessage("Choose an open item request from the marketplace first.");
    if (request.user_id === user.id) return setMessage("You cannot make an offer on your own request.");
    const profile = user.profile;
    if (!user.onboardingComplete || !user.name?.trim() || !profile?.phone?.trim() || !profile?.current_country?.trim() || !profile?.current_city?.trim() || !profile?.current_address?.trim()) { setStatus("error"); setMessage("Complete your profile before submitting an offer. Add your full name, active phone number, current country and city, and exact pickup address so the sender can safely accept you."); return; }
    const numericAmount = Number(amount);
    if (!Number.isFinite(numericAmount) || numericAmount <= 0) return setMessage("Enter a valid service amount.");
    setStatus("sending"); setMessage("");
    const { error } = await supabase.from("offers").insert({ request_id: request.id, traveler_id: user.id, amount_bdt: numericAmount, currency, estimated_delivery_at: date ? new Date(date).toISOString() : null, note: note.trim() || null, status: "pending" });
    if (error) { setStatus("error"); return setMessage(error.code === "23505" ? "You already have a pending offer on this request." : /row-level security|violates row-level/i.test(error.message) ? "Restricted account: your account cannot submit offers right now. Contact BridgeX support or an administrator for a review." : error.message); }
    await supabase.from("notifications").insert({ user_id: request.user_id, actor_id: user.id, type: "offer_received", title: "New offer received", body: `A traveler submitted an offer of ${currency} ${numericAmount.toLocaleString()} for ${request.title}.`, link: "/dashboard/offers", related_id: request.id });
    setStatus("sent"); setMessage("Offer submitted. Best wishes for a safe and successful handoff.");
  };

  const unavailable = !loading && !request;
  return <PublicLayout><main className="px-5 py-10 lg:px-8 lg:py-14"><div className="mx-auto max-w-[920px]"><Link href="/marketplace" className="inline-flex items-center gap-2 text-sm font-bold text-[#176447]"><ArrowLeft className="size-4" />Back to marketplace</Link><div className="mt-7 grid gap-5 lg:grid-cols-[0.85fr_1.15fr]"><aside className="rounded-3xl bg-[#172126] p-7 text-[#f7f5ef]"><span className="grid size-11 place-items-center rounded-2xl bg-[#2d8d62]"><Plane className="size-5" /></span><p className="mt-7 text-xs font-bold uppercase tracking-[0.15em] text-[#91e7bc]">{loading ? "Loading request" : request ? "Open item request" : "Request unavailable"}</p><h1 className="mt-3 font-display text-3xl font-bold tracking-[-0.05em]">{loading ? "Loading…" : request?.title ?? "Choose a marketplace request"}</h1>{request && <div className="mt-5 grid gap-2 text-sm text-[#c3d0c9]"><p>{request.purchase_city || "Purchase city"}, {request.purchase_country} → {request.destination_city}, {request.destination_country ?? "Destination"}</p><p>{request.weight_kg} kg · {request.size_description || "Size not stated"}</p><p>Required within: {request.delivery_required_days ? `${request.delivery_required_days} days` : "Not stated"}</p><p>Handling: {request.special_handling || "None stated"}</p><p>Sender budget: {currency} {Number(request.budget_bdt).toLocaleString()}</p></div>}<div className="mt-8 rounded-2xl bg-white/8 p-4"><div className="flex items-center gap-2"><ShieldCheck className="size-4 text-[#91e7bc]" /><p className="text-sm font-bold">Escrow begins after matching</p></div><p className="mt-2 text-xs leading-5 text-[#c3d0c9]">The sender compares real traveler offers. After a match, both people can follow the protected handoff and delivery stages.</p></div></aside><form onSubmit={submit} className="rounded-3xl border border-[#172126]/8 bg-white p-7"><p className="text-xs font-bold uppercase tracking-[0.16em] text-[#2d8d62]">Traveler offer</p><h2 className="mt-2 font-display text-3xl font-bold tracking-[-0.05em]">Make a clear proposal.</h2><p className="mt-3 text-sm leading-6 text-[#657275]">Only offer if you can fulfill the sender’s stated route, deadline, item requirements, and handling needs.</p>{unavailable ? <div className="mt-7 rounded-2xl bg-[#f6f4ee] p-5"><p className="text-sm font-semibold text-[#637073]">Select a current open request from the marketplace to make an offer.</p><Link href="/marketplace"><Button type="button" className="mt-4 rounded-xl bg-[#172126] font-bold">Browse open requests</Button></Link></div> : <div className="mt-7 grid gap-5"><div className="rounded-2xl border border-[#e4c984] bg-[#fff9ea] p-4 text-xs leading-5 text-[#6e5624]"><div className="flex gap-2 font-bold"><AlertTriangle className="size-4 shrink-0" />Before you continue</div><p className="mt-2">Make sure you can fulfill the sender requirements, delivery deadline, and product handling needs. Do not carry illegal, restricted, dangerous, or prohibited items. Prohibited activity can lead to post removal, account restriction or termination, transaction cancellation, and reports to appropriate authorities where required by law. Best wishes for a safe, lawful delivery.</p></div><div className="rounded-2xl border border-[#d6e7db] bg-[#f2faf4] p-4 text-sm text-[#36534a]"><p className="font-bold text-[#176447]">Complete details are required for acceptance</p><p className="mt-1 leading-6">Before you submit, make sure your profile contains your full name, active phone number, current country and city, and exact pickup address. These details are kept private and become visible only after a protected match is confirmed.</p><button type="button" onClick={() => setLocation("/onboarding")} className="mt-3 text-sm font-bold text-[#176447] hover:underline">Complete or update my details</button></div><div><Label>Service amount ({currency})</Label><div className="mt-2 flex h-11 items-center rounded-xl border border-[#d9d7cf] bg-[#faf9f5] px-3"><CircleDollarSign className="size-4 text-[#637073]" /><Input required inputMode="decimal" value={amount} onChange={event => setAmount(event.target.value)} placeholder="e.g. 850" className="border-0 bg-transparent shadow-none focus-visible:ring-0" /></div></div><div><Label>Estimated arrival</Label><div className="mt-2 flex h-11 items-center rounded-xl border border-[#d9d7cf] bg-[#faf9f5] px-3"><CalendarDays className="size-4 text-[#637073]" /><Input type="datetime-local" value={date} onChange={event => setDate(event.target.value)} className="border-0 bg-transparent shadow-none focus-visible:ring-0" /></div></div><div><Label>Offer note</Label><Textarea value={note} onChange={event => setNote(event.target.value)} placeholder="Share route timing, available capacity, and any important conditions." className="mt-2 min-h-28 rounded-xl" /></div><Button disabled={status === "sending" || loading} className="h-11 rounded-xl bg-[#172126] font-bold">{status === "sending" ? "Submitting offer…" : "Submit offer"}</Button>{message && <div className={`rounded-xl px-3 py-2 text-sm font-bold ${status === "sent" ? "bg-[#dff5ea] text-[#176447]" : "bg-[#f8e8e5] text-[#9b4b3e]"}`}>{status === "sent" && <CheckCircle2 className="mr-1 inline size-4" />}{message}</div>}</div>}</form></div></div></main></PublicLayout>;
}
