import PublicLayout from "@/components/bridgex/PublicLayout";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Textarea } from "@/components/ui/textarea";
import { supabase } from "@/lib/supabase";
import { Mail, Send, ShieldCheck } from "lucide-react";
import { useState } from "react";

export default function ContactPage() {
  const [form, setForm] = useState({ name: "", email: "", subject: "", message: "" }); const [busy, setBusy] = useState(false); const [result, setResult] = useState(""); const [failed, setFailed] = useState(false);
  const submit = async (event: React.FormEvent) => { event.preventDefault(); setBusy(true); setResult(""); setFailed(false); const { error } = await supabase.from("contact_enquiries").insert({ name: form.name.trim(), email: form.email.trim(), subject: form.subject.trim(), message: form.message.trim() }); setBusy(false); if (error) { setFailed(true); setResult(error.message); return; } setForm({ name: "", email: "", subject: "", message: "" }); setResult("Thank you. Your message was sent to the BridgeX administrator support panel."); };
  return <PublicLayout><main><section className="border-b border-[#172126]/8 bg-[#e8f3e9] px-5 py-16 lg:px-8 lg:py-20"><div className="mx-auto max-w-[920px]"><p className="text-xs font-bold uppercase tracking-[0.18em] text-[#23784f]">Support</p><h1 className="mt-4 font-display text-5xl font-bold tracking-[-0.06em] sm:text-6xl">Contact BridgeX</h1><p className="mt-6 max-w-2xl text-lg leading-8 text-[#526063]">Send a question about a listing, order, verification, or safety concern. Your enquiry is recorded for authorized administrator review.</p></div></section><section className="px-5 py-16 lg:px-8 lg:py-24"><div className="mx-auto grid max-w-[920px] gap-7 lg:grid-cols-[0.72fr_1fr]"><aside className="rounded-3xl bg-[#172126] p-7 text-[#f7f5ef]"><Mail className="size-7 text-[#91e7bc]" /><h2 className="mt-5 text-2xl font-bold">BridgeX support contact</h2><p className="mt-3 text-sm leading-6 text-[#c3d0c9]">For general platform questions, use the form. For immediate safety concerns, also use the report flow from the relevant member or order.</p><div className="mt-7 border-t border-white/10 pt-5"><p className="text-xs font-bold uppercase tracking-[0.13em] text-[#91e7bc]">Platform owner</p><p className="mt-2 font-bold">ABDULLAH BIN FAHAD</p><a href="mailto:abdullahbinfahad.abf@gmail.com" className="mt-1 inline-block text-sm text-[#d5e8dc] hover:text-white">abdullahbinfahad.abf@gmail.com</a></div><div className="mt-7 flex gap-2 text-xs leading-5 text-[#c3d0c9]"><ShieldCheck className="size-4 shrink-0 text-[#91e7bc]" />Do not send passwords, national-ID numbers, passports, or other unnecessary sensitive records through this form.</div></aside><form onSubmit={submit} className="rounded-3xl border border-[#172126]/8 bg-white p-7"><h2 className="text-2xl font-bold">Send an enquiry</h2><div className="mt-6 grid gap-5"><label className="grid gap-2 text-sm font-bold">Your name<Input required minLength={2} value={form.name} onChange={event => setForm({ ...form, name: event.target.value })} className="h-11 rounded-xl" /></label><label className="grid gap-2 text-sm font-bold">Email address<Input required type="email" value={form.email} onChange={event => setForm({ ...form, email: event.target.value })} className="h-11 rounded-xl" /></label><label className="grid gap-2 text-sm font-bold">Subject<Input required minLength={3} value={form.subject} onChange={event => setForm({ ...form, subject: event.target.value })} className="h-11 rounded-xl" /></label><label className="grid gap-2 text-sm font-bold">Message<Textarea required minLength={20} value={form.message} onChange={event => setForm({ ...form, message: event.target.value })} placeholder="Explain your question or concern without including passwords or unnecessary identity data." className="min-h-36 rounded-xl" /></label><Button disabled={busy} className="h-12 rounded-xl bg-[#172126] font-bold"><Send className="mr-2 size-4" />{busy ? "Sending…" : "Send to BridgeX support"}</Button>{result && <p className={`rounded-xl px-3 py-2 text-sm font-semibold ${failed ? "bg-[#f8e8e5] text-[#9b4b3e]" : "bg-[#dff5ea] text-[#176447]"}`}>{result}</p>}</div></form></div></section></main></PublicLayout>;
}
