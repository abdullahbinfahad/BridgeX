import { useAuth } from "@/_core/hooks/useAuth";
import DashboardLayout from "@/components/DashboardLayout";
import { Avatar, AvatarFallback, AvatarImage } from "@/components/ui/avatar";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { DropdownMenu, DropdownMenuContent, DropdownMenuItem, DropdownMenuTrigger } from "@/components/ui/dropdown-menu";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Textarea } from "@/components/ui/textarea";
import { compressImageForUpload } from "@/lib/fileUpload";
import { supabase } from "@/lib/supabase";
import GlobalVerification from "@/pages/GlobalVerification";
import Deals from "@/pages/Deals";
import ManagePosts from "@/pages/ManagePosts";
import { Banknote, CheckCircle2, ChevronRight, EllipsisVertical, FileUp, MapPin, PackageCheck, Plane, ShieldCheck, WalletCards } from "lucide-react";
import { useEffect, useMemo, useState } from "react";
import { Link, useLocation } from "wouter";

type MemberRequest = { id: string; title: string; status: string; purchase_country: string; destination_country: string | null; destination_city: string; created_at: string };
type MemberListing = { id: string; origin_country: string; origin_city: string; destination_country: string | null; destination_city: string; status: string; departure_at: string; available_weight_kg: number };
type MemberOrder = { id: string; match_id: string | null; reference: string; sender_id: string; traveler_id: string; amount_bdt: number; escrow_status: string; fulfillment_status: string; updated_at: string; last_traveler_update_at?: string | null; reminder_days?: number; match?: { sender_delivery_address: string; traveler_pickup_address: string } | null };
type MemberData = { requests: MemberRequest[]; listings: MemberListing[]; orders: MemberOrder[]; loading: boolean; error: string };

const emptyMemberData: MemberData = { requests: [], listings: [], orders: [], loading: true, error: "" };
const userName = (user: ReturnType<typeof useAuth>["user"]) => user?.name || user?.email?.split("@")[0] || "Member";
const formatMoney = (amount: number) => `৳ ${Number(amount ?? 0).toLocaleString()}`;
const statusLabel = (value: string) => value.replace(/_/g, " ").replace(/\b\w/g, (letter) => letter.toUpperCase());

function useMemberData(userId?: string): MemberData {
  const [data, setData] = useState<MemberData>(emptyMemberData);

  useEffect(() => {
    if (!userId) return;
    let alive = true;
    setData(previous => ({ ...previous, loading: true, error: "" }));
    void (async () => {
      const [requests, listings, orders] = await Promise.all([
        supabase.from("send_requests").select("id,title,status,purchase_country,destination_country,destination_city,created_at").eq("user_id", userId).order("created_at", { ascending: false }).limit(50),
        supabase.from("carry_listings").select("id,origin_country,origin_city,destination_country,destination_city,status,departure_at,available_weight_kg").eq("user_id", userId).order("departure_at", { ascending: true }).limit(50),
        supabase.from("orders").select("id,match_id,reference,sender_id,traveler_id,amount_bdt,escrow_status,fulfillment_status,updated_at,last_traveler_update_at,reminder_days").or(`sender_id.eq.${userId},traveler_id.eq.${userId}`).order("updated_at", { ascending: false }).limit(50),
      ]);
      if (!alive) return;
      const rawOrders = (orders.data ?? []) as MemberOrder[]; const matchIds = rawOrders.map(order => order.match_id).filter((id): id is string => Boolean(id)); const matchResult = matchIds.length ? await supabase.from("matches").select("id,sender_delivery_address,traveler_pickup_address").in("id", matchIds) : { data: [], error: null }; const matchById = new Map((matchResult.data ?? []).map((match: any) => [match.id, match]));
      const error = requests.error?.message || listings.error?.message || orders.error?.message || matchResult.error?.message || "";
      setData({ requests: (requests.data ?? []) as MemberRequest[], listings: (listings.data ?? []) as MemberListing[], orders: rawOrders.map(order => ({ ...order, match: order.match_id ? matchById.get(order.match_id) ?? null : null })), loading: false, error });
    })();
    return () => { alive = false; };
  }, [userId]);

  return data;
}

function Stat({ label, value, caption, icon: Icon, href }: { label: string; value: string; caption: string; icon: React.ElementType; href: string }) {
  return <Link href={href} className="group rounded-3xl border border-[#172126]/8 bg-white p-5 transition hover:-translate-y-0.5 hover:border-[#2d8d62]/45 hover:shadow-[0_14px_30px_rgba(32,45,41,0.08)]"><div className="flex items-start justify-between"><p className="text-sm font-bold text-[#637073]">{label}</p><span className="grid size-9 place-items-center rounded-xl bg-[#e7f4ea] text-[#176447]"><Icon className="size-4" /></span></div><p className="mt-5 font-display text-3xl font-bold tracking-[-0.05em]">{value}</p><p className="mt-1 flex items-center gap-1 text-xs text-[#738083]">{caption}<ChevronRight className="size-3 opacity-0 transition group-hover:opacity-100" /></p></Link>;
}

function Overview({ data }: { data: MemberData }) {
  const { user } = useAuth(); const greeting = new Date().getHours() < 12 ? "Good morning" : new Date().getHours() < 18 ? "Good afternoon" : "Good evening"; const activeOrders = data.orders.filter(order => !["released", "cancelled", "refunded"].includes(order.fulfillment_status) && !["released", "refunded"].includes(order.escrow_status)); const nextListing = data.listings.find(item => new Date(item.departure_at).getTime() >= Date.now()); const latestOrder = data.orders[0]; const overdueTravelerOrders = data.orders.filter(order => order.traveler_id === user?.id && ["matched", "china_pickup", "received", "in_transit"].includes(order.fulfillment_status) && Date.now() - new Date(order.last_traveler_update_at ?? order.updated_at).getTime() > 24 * 60 * 60 * 1000); const needsVerification = user?.verificationStatus !== "approved";
  return <><div className="flex flex-col justify-between gap-5 md:flex-row md:items-end"><div><p className="text-xs font-bold uppercase tracking-[0.16em] text-[#2d8d62]">Member workspace</p><h1 className="mt-2 font-display text-4xl font-bold tracking-[-0.055em]">{greeting}, {userName(user)}.</h1><p className="mt-2 text-sm leading-6 text-[#637073]">This workspace shows activity linked to your BridgeX account only.</p></div><div className="flex gap-2"><Link href="/create-request"><Button className="rounded-xl bg-[#172126] font-bold"><PackageCheck className="mr-2 size-4" />I need to send</Button></Link><Link href="/create-listing"><Button variant="outline" className="rounded-xl bg-white font-bold"><Plane className="mr-2 size-4" />List space</Button></Link></div></div>{overdueTravelerOrders.length > 0 && <div className="mt-5 rounded-2xl border border-[#e6c46b] bg-[#fff8e5] p-4"><p className="font-bold text-[#795500]">Traveler update needed</p><p className="mt-1 text-sm leading-6 text-[#765f26]">Update {overdueTravelerOrders.length === 1 ? overdueTravelerOrders[0].reference : `${overdueTravelerOrders.length} active orders`} today in Deals & chat. After three overdue days, the sender and administrators receive an attention alert.</p><Link href="/dashboard/deals"><Button size="sm" className="mt-3 rounded-lg bg-[#795500]">Open Deals & chat</Button></Link></div>}{data.error && <p className="mt-5 rounded-xl bg-[#f8e8e5] px-3 py-2 text-sm font-semibold text-[#9b4b3e]">{data.error}</p>}<div className="mt-7 grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><Stat href="/dashboard/orders" label="Active orders" value={data.loading ? "—" : String(activeOrders.length)} caption={activeOrders.length ? "Protected order in progress" : "No active protected orders"} icon={PackageCheck} /><Stat href="/dashboard/requests" label="My requests" value={data.loading ? "—" : String(data.requests.length)} caption={data.requests.length ? `${data.requests.filter(item => item.status === "open").length} currently open` : "No requests posted yet"} icon={PackageCheck} /><Stat href="/dashboard/listings" label="Carry listings" value={data.loading ? "—" : String(data.listings.length)} caption={nextListing ? `Next: ${new Date(nextListing.departure_at).toLocaleDateString()}` : "No upcoming carry listing"} icon={Plane} /><Stat href="/dashboard/wallet" label="Escrow activity" value={data.loading ? "—" : String(data.orders.length)} caption={data.orders.length ? "From your protected orders" : "No order activity yet"} icon={WalletCards} /></div><div className={`mt-7 grid gap-5 ${needsVerification ? "xl:grid-cols-[1.45fr_0.85fr]" : ""}`}>{latestOrder ? <LiveOrderSummary order={latestOrder} /> : <EmptyOrderSummary />}{needsVerification && <div className="rounded-3xl bg-[#172126] p-6 text-[#f7f5ef]"><ShieldCheck className="size-5 text-[#91e7bc]" /><h2 className="mt-6 font-display text-2xl font-bold tracking-[-0.04em]">Verification improves trust without pausing your account.</h2><p className="mt-3 text-sm leading-6 text-[#c3d0c9]">Complete the required identity documents when ready. Your account stays active while an administrator reviews the submission.</p><Link href="/dashboard/verification"><Button className="mt-6 rounded-xl bg-[#91e7bc] font-bold text-[#172126] hover:bg-[#b7efcc]">Open verification <ChevronRight className="ml-1 size-4" /></Button></Link></div>}</div></>;
}

function OrderMilestones({ order }: { order: MemberOrder }) {
  const { user } = useAuth();
  const isTraveler = order.traveler_id === user?.id;
  const stages = isTraveler ? ["Offer", "Matched", "China pickup", "Received", "Transit", "Delivered", "Released"] : ["Offer", "Matched", "China send", "Traveler received", "Transit", "Delivered", "Released"];
  const fulfillment = order.fulfillment_status.toLowerCase();
  const escrow = order.escrow_status.toLowerCase();
  const progress = escrow === "released" ? 6 : fulfillment === "delivered" || fulfillment === "completed" ? 5 : fulfillment === "in_transit" ? 4 : ["received", "handoff"].includes(fulfillment) ? 3 : ["china_pickup", "purchased"].includes(fulfillment) ? 2 : fulfillment === "matched" || ["funded", "held"].includes(escrow) ? 1 : 0;
  return <div className="mt-6"><p className="text-xs font-bold text-[#536263]">{isTraveler ? "Traveler view" : "Sender view"}</p><div className="mt-3 grid grid-cols-7 gap-1">{stages.map((stage, index) => <div key={stage} className="min-w-0"><span className={`grid size-6 place-items-center rounded-full text-[10px] font-bold ${index <= progress ? "bg-[#2d8d62] text-white" : "bg-[#eceae3] text-[#788481]"}`}>{index <= progress ? <CheckCircle2 className="size-3.5" /> : index + 1}</span><p className="mt-1.5 text-[10px] font-bold leading-3 text-[#667477]">{stage}</p></div>)}</div></div>;
}

function LiveOrderSummary({ order }: { order: MemberOrder }) {
  return <div className="rounded-3xl border border-[#172126]/8 bg-white p-6"><div className="flex flex-wrap items-start justify-between gap-3"><div><p className="text-xs font-bold uppercase tracking-[0.14em] text-[#2d8d62]">Active order · {order.reference}</p><h2 className="mt-2 text-lg font-bold">Protected order</h2><p className="mt-1 text-sm text-[#637073]">Last updated {new Date(order.updated_at).toLocaleString()}</p></div><Badge className="rounded-full bg-[#dff5ea] text-[#176447] hover:bg-[#dff5ea]">{statusLabel(order.escrow_status)}</Badge></div><OrderMilestones order={order} /><div className="mt-6 flex flex-wrap items-center justify-between gap-3 rounded-2xl bg-[#f4f2eb] p-4"><div><p className="text-xs font-bold text-[#536263]">Protected amount</p><p className="mt-1 text-lg font-bold">{formatMoney(order.amount_bdt)}</p><p className="mt-1 text-xs text-[#637073]">Fulfillment: {statusLabel(order.fulfillment_status)}</p></div><Link href="/dashboard/orders"><Button className="rounded-lg bg-[#172126] font-bold">View order <ChevronRight className="ml-1.5 size-4" /></Button></Link></div></div>;
}

function EmptyOrderSummary() {
  return <div className="rounded-3xl border border-dashed border-[#172126]/18 bg-white p-8"><Banknote className="size-7 text-[#2d8d62]" /><h2 className="mt-5 text-lg font-bold">No protected orders yet.</h2><p className="mt-2 max-w-md text-sm leading-6 text-[#637073]">When a sender and traveler confirm a match, the protected order and its escrow status will appear here.</p><Link href="/marketplace"><Button variant="outline" className="mt-6 rounded-xl bg-white font-bold">Browse marketplace</Button></Link></div>;
}

function PostActions({ kind, id, status }: { kind: "request" | "listing"; id: string; status: string }) {
  const remove = async () => { if (!window.confirm("Delete this open post? This cannot be undone.")) return; const table = kind === "request" ? "send_requests" : "carry_listings"; const { error } = await supabase.from(table).delete().eq("id", id); if (error) return window.alert(error.message); window.location.reload(); };
  return <DropdownMenu><DropdownMenuTrigger asChild><Button size="icon" variant="outline" className="size-9 rounded-lg bg-white" aria-label="Post actions"><EllipsisVertical className="size-4" /></Button></DropdownMenuTrigger><DropdownMenuContent align="end"><DropdownMenuItem onSelect={() => window.location.assign(`/post/${kind}/${id}`)}>Open post</DropdownMenuItem><DropdownMenuItem onSelect={() => window.location.assign(`/dashboard/manage-posts?edit=${encodeURIComponent(id)}`)}>Edit post</DropdownMenuItem>{status === "open" && <DropdownMenuItem className="text-[#9b4b3e] focus:text-[#9b4b3e]" onSelect={() => void remove()}>Delete post</DropdownMenuItem>}</DropdownMenuContent></DropdownMenu>;
}

function TravelerOrderUpdate({ order }: { order: MemberOrder }) {
  const { user } = useAuth(); const [busy, setBusy] = useState(false); const isTraveler = order.traveler_id === user?.id; if (!isTraveler) return null;
  const update = async (status: string) => { setBusy(true); const { error } = await supabase.rpc("update_bridgex_traveler_order", { p_order_id: order.id, p_fulfillment_status: status }); setBusy(false); if (error) return window.alert(error.message); window.location.reload(); };
  return <div className="mt-3 flex flex-wrap gap-1.5">{[["china_pickup", "Pickup"], ["received", "Received"], ["in_transit", "Transit"], ["delivered", "Delivered"]].map(([value, label]) => <Button key={value} disabled={busy || order.fulfillment_status === value} onClick={() => void update(value)} size="sm" variant="outline" className="h-8 rounded-lg bg-white text-xs">{label}</Button>)}</div>;
}

function SenderReceiptRelease({ order }: { order: MemberOrder }) {
  const { user } = useAuth(); const [busy, setBusy] = useState(false); const isSender = order.sender_id === user?.id; const eligible = isSender && order.fulfillment_status === "delivered" && order.escrow_status !== "released";
  if (!eligible) return null;
  const release = async () => { if (!window.confirm("Confirm that you received this product? This releases the protected order, removes the original post photos and video to save storage, and keeps the order history.")) return; setBusy(true); const { error } = await supabase.rpc("confirm_bridgex_sender_delivery", { p_order_id: order.id }); setBusy(false); if (error) return window.alert(error.message); window.location.assign("/dashboard/completed"); };
  return <Button disabled={busy} onClick={() => void release()} size="sm" className="mt-3 h-8 rounded-lg bg-[#176447] text-xs">{busy ? "Completing…" : "Product received & release"}</Button>;
}

function MyRequests({ data }: { data: MemberData }) {
  return <ListPage title="My item requests" copy="Open a post for its complete details, then use the menu to edit or delete your open request." action="Post a request" href="/create-request">{data.requests.length ? data.requests.map(item => <article key={item.id} className="flex flex-wrap items-center justify-between gap-3 rounded-2xl border border-[#172126]/8 bg-white p-4"><Link href={`/post/request/${item.id}`} className="min-w-0 flex-1"><p className="font-bold hover:text-[#176447]">{item.title}</p><p className="mt-1 text-sm text-[#667477]">{item.purchase_country} → {item.destination_country ?? "Destination pending"} · {item.destination_city}</p></Link><div className="flex items-center gap-2"><Badge className="bg-[#e7f4ea] text-[#176447] hover:bg-[#e7f4ea]">{statusLabel(item.status)}</Badge><PostActions kind="request" id={item.id} status={item.status} /></div></article>) : <EmptyContent copy="You have not posted an item request yet." href="/create-request" label="Post your first request" />}</ListPage>;
}

function MyListings({ data }: { data: MemberData }) {
  return <ListPage title="My carry space" copy="Open a listing for its route and media, then use the menu to edit or delete your open carry-space post." action="List space" href="/create-listing">{data.listings.length ? data.listings.map(item => <article key={item.id} className="flex flex-wrap items-center justify-between gap-3 rounded-2xl border border-[#172126]/8 bg-white p-4"><Link href={`/post/listing/${item.id}`} className="min-w-0 flex-1"><p className="font-bold hover:text-[#176447]">{item.origin_city}, {item.origin_country} → {item.destination_city}, {item.destination_country ?? "Destination pending"}</p><p className="mt-1 text-sm text-[#667477]">{item.available_weight_kg} kg · Departure {new Date(item.departure_at).toLocaleDateString()}</p></Link><div className="flex items-center gap-2"><Badge className="bg-[#e7f4ea] text-[#176447] hover:bg-[#e7f4ea]">{statusLabel(item.status)}</Badge><PostActions kind="listing" id={item.id} status={item.status} /></div></article>) : <EmptyContent copy="You have not listed carry space yet." href="/create-listing" label="List carry space" />}</ListPage>;
}

function Orders({ data }: { data: MemberData }) {
  const { user } = useAuth(); const activeOrders = data.orders.filter(order => !["completed", "released", "cancelled", "refunded"].includes(order.fulfillment_status) && order.escrow_status !== "released");
  return <ListPage title="Protected orders" copy="Only active orders where you are the sender or traveler appear here. Travelers update delivery; senders confirm receipt only after delivered." action="Completed orders" href="/dashboard/completed">{activeOrders.length ? activeOrders.map(order => { const travelerView = order.traveler_id === user?.id; const location = travelerView ? order.match?.sender_delivery_address : order.match?.traveler_pickup_address; return <article key={order.id} className="flex flex-wrap items-start justify-between gap-3 rounded-2xl border border-[#172126]/8 bg-white p-4"><div className="min-w-0 flex-1"><p className="font-bold">{order.reference}</p><p className="mt-1 text-sm text-[#667477]">{formatMoney(order.amount_bdt)} · {statusLabel(order.fulfillment_status)} · Updated {new Date(order.updated_at).toLocaleDateString()}</p><p className="mt-2 flex items-start gap-1.5 text-xs font-semibold leading-5 text-[#526063]"><MapPin className="mt-0.5 size-3.5 shrink-0 text-[#2d8d62]" />{travelerView ? "Delivery destination" : "Traveler pickup location"}: {location || "Open the protected deal to complete or review this address."}</p><TravelerOrderUpdate order={order} /><SenderReceiptRelease order={order} /></div><div className="flex items-center gap-2"><Badge className="bg-[#e7f4ea] text-[#176447] hover:bg-[#e7f4ea]">{statusLabel(order.escrow_status)}</Badge>{order.match_id && <Link href={`/dashboard/deals?match=${order.match_id}`}><Button size="sm" variant="outline" className="rounded-lg bg-white">Open</Button></Link>}</div></article>; }) : <EmptyContent copy="No active protected order is connected to your account. Completed orders are kept separately." href="/dashboard/completed" label="Open completed orders" />}</ListPage>;
}

function CompletedOrders({ data }: { data: MemberData }) {
  const completed = data.orders.filter(order => order.fulfillment_status === "completed" || order.escrow_status === "released");
  return <ListPage title="Completed orders" copy="Protected order details, references, and milestones stay available here after confirmation. Original public post photos and video are removed to save storage." action="Active orders" href="/dashboard/orders">{completed.length ? completed.map(order => <article key={order.id} className="flex flex-wrap items-start justify-between gap-3 rounded-2xl border border-[#172126]/8 bg-white p-4"><div className="min-w-0 flex-1"><p className="font-bold">{order.reference}</p><p className="mt-1 text-sm text-[#667477]">{formatMoney(order.amount_bdt)} · Completed {new Date(order.updated_at).toLocaleDateString()}</p><p className="mt-2 text-xs font-semibold text-[#176447]">Order history retained · Original post media removed</p></div><div className="flex items-center gap-2"><Badge className="bg-[#e7f4ea] text-[#176447] hover:bg-[#e7f4ea]">Released</Badge>{order.match_id && <Link href={`/dashboard/deals?match=${order.match_id}`}><Button size="sm" variant="outline" className="rounded-lg bg-white">View deal</Button></Link>}</div></article>) : <EmptyContent copy="Completed orders will appear here after the sender confirms receipt and releases the protected order." href="/dashboard/orders" label="Open active orders" />}</ListPage>;
}

function Wallet({ data }: { data: MemberData }) {
  const totalHeld = data.orders.filter(order => !["released", "refunded"].includes(order.escrow_status)).reduce((total, order) => total + Number(order.amount_bdt || 0), 0);
  return <><div><p className="text-xs font-bold uppercase tracking-[0.16em] text-[#2d8d62]">Escrow activity</p><h1 className="mt-2 font-display text-4xl font-bold tracking-[-0.055em]">Order-linked funds only.</h1><p className="mt-2 text-sm text-[#637073]">BridgeX displays protected order status; it does not invent wallet balances.</p></div><div className="mt-7 grid gap-5 lg:grid-cols-[0.8fr_1.2fr]"><div className="rounded-3xl bg-[#172126] p-7 text-[#f7f5ef]"><WalletCards className="size-7 text-[#91e7bc]" /><p className="mt-10 text-sm font-bold text-[#c3d0c9]">In active escrow</p><p className="mt-1 font-display text-5xl font-bold tracking-[-0.06em]">{formatMoney(totalHeld)}</p><p className="mt-5 text-xs leading-5 text-[#b9cac1]">This total is calculated from your existing protected orders and is not a withdrawable account balance.</p></div><div className="rounded-3xl border border-[#172126]/8 bg-white p-6"><h2 className="font-bold">Your order activity</h2><div className="mt-5 grid gap-3">{data.orders.length ? data.orders.map(order => <div key={order.id} className="flex items-center justify-between rounded-xl bg-[#f6f4ee] p-4"><div><p className="text-sm font-bold">{order.reference}</p><p className="mt-1 text-xs text-[#6d797c]">{statusLabel(order.escrow_status)} · {statusLabel(order.fulfillment_status)}</p></div><strong>{formatMoney(order.amount_bdt)}</strong></div>) : <p className="rounded-xl bg-[#f6f4ee] p-4 text-sm text-[#667477]">No order activity yet.</p>}</div></div></div></>;
}

type OwnerOffer = { id: string; request_id: string; traveler_id: string; amount_bdt: number; estimated_delivery_at: string | null; note: string | null; status: string; created_at: string; request: { title: string } | null };
type OwnerInterest = { id: string; listing_id: string; sender_id: string; note: string | null; status: string; created_at: string; listing: { origin_city: string; destination_city: string } | null };
function Offers() {
  const { user } = useAuth(); const [offers, setOffers] = useState<OwnerOffer[]>([]); const [interests, setInterests] = useState<OwnerInterest[]>([]); const [loading, setLoading] = useState(true); const [message, setMessage] = useState("");
  const load = async () => { if (!user) return; setLoading(true); const [myRequests, myListings] = await Promise.all([supabase.from("send_requests").select("id,title").eq("user_id", user.id).limit(100), supabase.from("carry_listings").select("id,origin_city,destination_city").eq("user_id", user.id).limit(100)]); const requestIds = (myRequests.data ?? []).map(item => item.id); const listingIds = (myListings.data ?? []).map(item => item.id); const [offerRows, interestRows] = await Promise.all([requestIds.length ? supabase.from("offers").select("id,request_id,traveler_id,amount_bdt,estimated_delivery_at,note,status,created_at").in("request_id", requestIds).order("created_at", { ascending: false }) : Promise.resolve({ data: [], error: null }), listingIds.length ? supabase.from("listing_interests").select("id,listing_id,sender_id,note,status,created_at").in("listing_id", listingIds).order("created_at", { ascending: false }) : Promise.resolve({ data: [], error: null })]); const requestLookup = new Map((myRequests.data ?? []).map(item => [item.id, { title: item.title }])); const listingLookup = new Map((myListings.data ?? []).map(item => [item.id, { origin_city: item.origin_city, destination_city: item.destination_city }])); setOffers((offerRows.data ?? []).map((item: any) => ({ ...item, request: requestLookup.get(item.request_id) ?? null }))); setInterests((interestRows.data ?? []).map((item: any) => ({ ...item, listing: listingLookup.get(item.listing_id) ?? null }))); setLoading(false); };
  useEffect(() => { void load(); }, [user?.id]);
  const offerGroups = useMemo(() => offers.reduce<Record<string, OwnerOffer[]>>((groups, offer) => { (groups[offer.request_id] ??= []).push(offer); return groups; }, {}), [offers]);
  const update = async (table: "offers" | "listing_interests", id: string, status: "accepted" | "rejected") => { if (!user) return; setMessage(""); if (status === "accepted") { const { data, error } = await supabase.rpc("accept_bridgex_response", { p_kind: table === "offers" ? "offer" : "interest", p_response_id: id }); if (error) { setMessage(error.message); return; } setMessage("Response accepted. This post is now removed from the public feed. Open Deals & chat to see protected contact details and continue the conversation."); await load(); window.location.assign(`/dashboard/deals?match=${data}`); return; } const response = table === "offers" ? offers.find(item => item.id === id) : interests.find(item => item.id === id); const { error } = await supabase.from(table).update({ status, updated_at: new Date().toISOString() }).eq("id", id); if (error) { setMessage(error.message); return; } if (response) { const recipientId = table === "offers" ? (response as OwnerOffer).traveler_id : (response as OwnerInterest).sender_id; const relatedId = table === "offers" ? (response as OwnerOffer).request_id : (response as OwnerInterest).listing_id; await supabase.from("notifications").insert({ user_id: recipientId, actor_id: user.id, type: `${table}_rejected`, title: "Your response was declined", body: "The post owner declined this response. You can browse other live posts in the marketplace.", link: "/dashboard/offers", related_id: relatedId }); } setMessage("Response declined."); await load(); };
  const responseCard = (kind: "offer" | "interest", item: OwnerOffer | OwnerInterest) => { const isOffer = kind === "offer"; const offer = item as OwnerOffer; const interest = item as OwnerInterest; const title = isOffer ? offer.request?.title || "Item request" : `${interest.listing?.origin_city || "Route"} → ${interest.listing?.destination_city || "Destination"}`; return <article key={item.id} className="rounded-2xl border border-[#172126]/8 bg-white p-5"><div className="flex flex-wrap items-start justify-between gap-3"><div><p className="text-xs font-bold uppercase tracking-[0.14em] text-[#2d8d62]">{isOffer ? "Traveler offer" : "Sender interest"}</p><h2 className="mt-1 font-bold">{title}</h2><p className="mt-1 text-xs text-[#6b787a]">Received {new Date(item.created_at).toLocaleString()}</p></div><Badge className="bg-[#e7f4ea] text-[#176447] hover:bg-[#e7f4ea]">{statusLabel(item.status)}</Badge></div>{isOffer && <p className="mt-4 rounded-xl bg-[#f5f3ed] p-3 text-sm"><strong>{formatMoney(offer.amount_bdt)}</strong>{offer.estimated_delivery_at ? ` · estimated ${new Date(offer.estimated_delivery_at).toLocaleDateString()}` : ""}</p>}<p className="mt-3 text-sm leading-6 text-[#596669]">{item.note || "No additional note was provided."}</p>{item.status === "pending" && <div className="mt-4 flex flex-wrap gap-2"><Button onClick={() => update(isOffer ? "offers" : "listing_interests", item.id, "accepted")} size="sm" className="rounded-lg bg-[#176447]">Accept</Button><Button onClick={() => update(isOffer ? "offers" : "listing_interests", item.id, "rejected")} size="sm" variant="outline" className="rounded-lg bg-white">Decline</Button></div>}</article>; };
  return <ListPage title="Incoming offers and interests" copy="Each post has its own response group, so you can compare people without mixing different deliveries." action="I need to send" href="/create-request"><div className="grid gap-5">{message && <p className="rounded-xl bg-[#f4f2eb] px-3 py-2 text-sm font-semibold text-[#536164]">{message}</p>}{loading ? <p className="rounded-2xl bg-white p-6 text-sm font-semibold text-[#647174]">Loading responses…</p> : offers.length || interests.length ? <><section><h2 className="mb-3 text-sm font-bold uppercase tracking-[0.12em] text-[#617073]">Offers on my item requests</h2><div className="grid gap-5">{Object.values(offerGroups).length ? Object.values(offerGroups).map(group => <div key={group[0].request_id} className="rounded-3xl border border-[#172126]/8 bg-[#f7f5ef] p-4"><div className="flex items-center justify-between gap-3"><div><p className="text-xs font-bold uppercase tracking-[0.12em] text-[#2d8d62]">My request</p><h3 className="mt-1 font-bold">{group[0].request?.title || "Item request"}</h3></div><Badge className="bg-white text-[#176447] hover:bg-white">{group.length} offer{group.length === 1 ? "" : "s"}</Badge></div><div className="mt-3 grid gap-3">{group.map(item => responseCard("offer", item))}</div></div>) : <p className="rounded-2xl bg-white p-5 text-sm text-[#637073]">No traveler offers yet.</p>}</div></section><section><h2 className="mb-3 text-sm font-bold uppercase tracking-[0.12em] text-[#617073]">Interest in my carry space</h2><div className="grid gap-5">{interests.length ? interests.map(item => <div key={item.id}>{responseCard("interest", item)}</div>) : <p className="rounded-2xl bg-white p-5 text-sm text-[#637073]">No sender interest yet.</p>}</div></section></> : <EmptyContent copy="Post an item request or carry space, then incoming offers and interests will appear here." href="/marketplace" label="Open marketplace" />}</div></ListPage>;
}

function Reviews() { return <ListPage title="Reviews" copy="Reviews are available after a completed protected order." action="View orders" href="/dashboard/orders"><EmptyContent copy="No post-order reviews are available for this account yet." href="/dashboard/orders" label="View orders" /></ListPage>; }

function ListPage({ title, copy, action, href, children }: { title: string; copy: string; action: string; href: string; children: React.ReactNode }) {
  return <><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="text-xs font-bold uppercase tracking-[0.16em] text-[#2d8d62]">Workspace</p><h1 className="mt-2 font-display text-4xl font-bold tracking-[-0.055em]">{title}</h1><p className="mt-2 text-sm text-[#637073]">{copy}</p></div><Link href={href}><Button className="rounded-xl bg-[#172126] font-bold">{action}</Button></Link></div><div className="mt-7 grid gap-3">{children}</div></>;
}

function EmptyContent({ copy, href, label }: { copy: string; href: string; label: string }) {
  return <div className="rounded-3xl border border-dashed border-[#172126]/18 bg-white p-10 text-center"><PackageCheck className="mx-auto size-8 text-[#2d8d62]" /><p className="mx-auto mt-4 max-w-md text-sm leading-6 text-[#637073]">{copy}</p><Link href={href}><Button variant="outline" className="mt-5 rounded-xl bg-white font-bold">{label}</Button></Link></div>;
}

function SettingsView() {
  const { user, refresh } = useAuth();
  const [form, setForm] = useState({ fullName: "", phone: "", bio: "", currentCountry: "", currentCity: "", currentAddress: "", homeCountry: "", homeCity: "", homeAddress: "", chinaAddress: "" });
  const [avatar, setAvatar] = useState<File | null>(null);
  const [message, setMessage] = useState("");
  const [saving, setSaving] = useState(false);

  useEffect(() => {
    if (!user) return;
    setForm({ fullName: user.name ?? "", phone: user.profile?.phone ?? "", bio: user.profile?.bio ?? "", currentCountry: user.profile?.current_country ?? "", currentCity: user.profile?.current_city ?? "", currentAddress: user.profile?.current_address ?? "", homeCountry: user.profile?.home_country ?? "", homeCity: user.profile?.home_city ?? "", homeAddress: user.profile?.home_address ?? "", chinaAddress: user.profile?.china_address ?? "" });
  }, [user]);

  const avatarUrl = avatar ? URL.createObjectURL(avatar) : user?.avatarUrl;
  const update = (key: keyof typeof form, value: string) => setForm(current => ({ ...current, [key]: value }));
  const save = async () => {
    if (!user) return;
    if (!form.fullName.trim() || !form.phone.trim() || !form.currentCountry.trim() || !form.currentCity.trim() || !form.currentAddress.trim() || !form.homeCountry.trim() || !form.homeCity.trim() || !form.homeAddress.trim()) return setMessage("Complete your phone, exact current location, and exact home delivery location before saving.");
    if (form.currentCountry.trim().toLowerCase() === "china" && !form.chinaAddress.trim()) return setMessage("Add your exact China address when your current country is China.");
    setSaving(true); setMessage("");
    let avatarPath: string | null | undefined;
    if (avatar) {
      const compressed = await compressImageForUpload(avatar);
      if (compressed.size > 2 * 1024 * 1024) { setSaving(false); return setMessage("Profile image is still larger than 2 MB after compression."); }
      const safeName = compressed.name.replace(/[^a-zA-Z0-9._-]/g, "_");
      const path = `${user.id}/${crypto.randomUUID()}-${safeName}`;
      const { error: uploadError } = await supabase.storage.from("profile-avatars").upload(path, compressed, { contentType: compressed.type, upsert: false });
      if (uploadError) { setSaving(false); return setMessage(uploadError.message); }
      avatarPath = path;
    }
    const { error } = await supabase.from("users").update({ full_name: form.fullName.trim(), phone: form.phone.trim(), bio: form.bio.trim() || null, current_country: form.currentCountry.trim(), current_city: form.currentCity.trim(), current_address: form.currentAddress.trim(), home_country: form.homeCountry.trim(), home_city: form.homeCity.trim(), home_address: form.homeAddress.trim(), china_address: form.chinaAddress.trim() || null, ...(avatarPath ? { avatar_path: avatarPath } : {}) }).eq("id", user.id);
    setSaving(false);
    if (error) return setMessage(error.message);
    await refresh();
    setAvatar(null);
    setMessage("Profile details saved.");
  };

  return <div><p className="text-xs font-bold uppercase tracking-[0.16em] text-[#2d8d62]">Account settings</p><h1 className="mt-2 font-display text-4xl font-bold tracking-[-0.055em]">Edit your profile.</h1><p className="mt-2 max-w-2xl text-sm leading-6 text-[#637073]">Keep your exact current and home details accurate. They support safer matching, verification, and incident handling.</p><div className="mt-7 max-w-3xl rounded-3xl border border-[#172126]/8 bg-white p-6"><div className="flex flex-wrap items-center gap-4 border-b border-[#172126]/8 pb-6"><Avatar className="size-16 border border-[#dce6df]"><AvatarImage src={avatarUrl} alt="Profile avatar" /><AvatarFallback className="bg-[#dff5ea] text-lg font-bold text-[#176447]">{userName(user).charAt(0).toUpperCase()}</AvatarFallback></Avatar><div><p className="font-bold">Profile photo</p><p className="mt-1 text-xs text-[#687579]">JPG, PNG, or WEBP; images are compressed before upload.</p><label className="mt-3 inline-flex cursor-pointer items-center rounded-lg border border-[#172126]/12 bg-white px-3 py-2 text-xs font-bold"><FileUp className="mr-1.5 size-3.5" />Choose photo<input className="sr-only" type="file" accept="image/jpeg,image/png,image/webp" onChange={event => setAvatar(event.target.files?.[0] ?? null)} /></label></div></div><div className="mt-6 grid gap-4 sm:grid-cols-2"><div><Label>Display name</Label><Input value={form.fullName} onChange={event => update("fullName", event.target.value)} className="mt-2 h-11 rounded-xl" /></div><div><Label>Phone number (required for accepted deals)</Label><Input value={form.phone} onChange={event => update("phone", event.target.value)} className="mt-2 h-11 rounded-xl" /></div><div className="sm:col-span-2"><Label>Short profile bio (optional)</Label><Textarea value={form.bio} onChange={event => update("bio", event.target.value)} className="mt-2 min-h-24 rounded-xl" /></div></div><h2 className="mt-8 border-t border-[#172126]/8 pt-6 font-bold">Current exact location</h2><div className="mt-4 grid gap-4 sm:grid-cols-2"><div><Label>Current country</Label><Input value={form.currentCountry} onChange={event => update("currentCountry", event.target.value)} className="mt-2 h-11 rounded-xl" /></div><div><Label>Current city</Label><Input value={form.currentCity} onChange={event => update("currentCity", event.target.value)} className="mt-2 h-11 rounded-xl" /></div><div className="sm:col-span-2"><Label>Current pickup address</Label><Input value={form.currentAddress} onChange={event => update("currentAddress", event.target.value)} className="mt-2 h-11 rounded-xl" /></div><div className="sm:col-span-2"><Label>Exact China address (required when current country is China)</Label><Input value={form.chinaAddress} onChange={event => update("chinaAddress", event.target.value)} className="mt-2 h-11 rounded-xl" /></div></div><h2 className="mt-8 border-t border-[#172126]/8 pt-6 font-bold">Home delivery location</h2><div className="mt-4 grid gap-4 sm:grid-cols-2"><div><Label>Home country</Label><Input value={form.homeCountry} onChange={event => update("homeCountry", event.target.value)} className="mt-2 h-11 rounded-xl" /></div><div><Label>Home city</Label><Input value={form.homeCity} onChange={event => update("homeCity", event.target.value)} className="mt-2 h-11 rounded-xl" /></div><div className="sm:col-span-2"><Label>Exact home delivery address</Label><Input value={form.homeAddress} onChange={event => update("homeAddress", event.target.value)} className="mt-2 h-11 rounded-xl" /></div></div><Button onClick={save} disabled={saving} className="mt-7 rounded-xl bg-[#172126] font-bold">{saving ? "Saving profile…" : "Save profile"}</Button>{message && <p className={`mt-4 rounded-xl px-3 py-2 text-sm font-semibold ${message === "Profile details saved." ? "bg-[#dff5ea] text-[#176447]" : "bg-[#f8e8e5] text-[#9b4b3e]"}`}>{message}</p>}</div></div>;
}

export default function Workspace() {
  const { user } = useAuth();
  const [location] = useLocation();
  const section = location.split("/")[2] ?? "overview";
  const data = useMemberData(user?.id);
  useEffect(() => { if (user) void supabase.rpc("process_bridgex_overdue_traveler_reminders"); }, [user?.id]);
  const content = section === "verification" ? <GlobalVerification /> : section === "wallet" ? <Wallet data={data} /> : section === "orders" ? <Orders data={data} /> : section === "completed" ? <CompletedOrders data={data} /> : section === "offers" ? <Offers /> : section === "deals" ? <Deals /> : section === "manage-posts" ? <ManagePosts /> : section === "settings" ? <SettingsView /> : section === "requests" ? <MyRequests data={data} /> : section === "listings" ? <MyListings data={data} /> : section === "reviews" ? <Reviews /> : <Overview data={data} />;
  return <DashboardLayout>{content}</DashboardLayout>;
}
