import { useAuth } from "@/_core/hooks/useAuth";
import PublicLayout from "@/components/bridgex/PublicLayout";
import { MemberVerificationBadge } from "@/components/bridgex/VerifiedBadge";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { supabase } from "@/lib/supabase";
import { CalendarDays, Heart, MapPin, PackageCheck, Plane, Search, Weight } from "lucide-react";
import { useEffect, useMemo, useRef, useState } from "react";
import { Link } from "wouter";

type View = "requests" | "flights";
type MarketItem = { id: string; ownerId: string; title: string; category: string; productCategories: string[]; route: string; location: string; weight: string; budget: string; date: string; tag: string; imageUrl?: string | null; posterName: string; posterVerified: boolean };
const PAGE_SIZE = 36;

export default function Marketplace() {
  const { user } = useAuth(); const initialView: View = new URLSearchParams(window.location.search).get("view") === "flights" ? "flights" : "requests";
  const [view, setView] = useState<View>(initialView); const [query, setQuery] = useState(""); const [category, setCategory] = useState("All categories"); const [saved, setSaved] = useState<Set<string>>(new Set()); const [items, setItems] = useState<MarketItem[]>([]); const [loading, setLoading] = useState(true); const [loadingMore, setLoadingMore] = useState(false); const [error, setError] = useState(""); const [page, setPage] = useState(0); const [hasMore, setHasMore] = useState(false); const activeObjectUrls = useRef<string[]>([]);
  const clearObjectUrls = () => { activeObjectUrls.current.forEach(url => URL.revokeObjectURL(url)); activeObjectUrls.current = []; };
  const addImages = async (rows: any[], imagePath: (item: any) => string | null) => Promise.all(rows.map(async item => { let imageUrl: string | null = null; const path = imagePath(item); if (path) { const { data: imageBlob } = await supabase.storage.from("request-media").download(path); if (imageBlob?.type.startsWith("image/")) { imageUrl = URL.createObjectURL(imageBlob); activeObjectUrls.current.push(imageUrl); } } return { ...item, imageUrl }; }));
  const load = async (reset = false) => { const nextPage = reset ? 0 : page; if (reset) { setLoading(true); setError(""); clearObjectUrls(); } else setLoadingMore(true); const start = nextPage * PAGE_SIZE; let rows: any[] = []; let fetchError: any = null;
    if (view === "requests") { const result = await supabase.from("send_requests").select("id,user_id,title,category,categories,purchase_country,destination_country,destination_district,destination_city,weight_kg,budget_bdt,image_path,media_paths,created_at").eq("status", "open").order("created_at", { ascending: false }).range(start, start + PAGE_SIZE - 1); rows = result.data ?? []; fetchError = result.error; } else { const result = await supabase.from("carry_listings").select("id,user_id,origin_country,origin_city,destination_country,destination_district,destination_city,transport_mode,available_weight_kg,price_bdt,pricing_mode,departure_at,created_at,media_paths").eq("status", "open").order("created_at", { ascending: false }).range(start, start + PAGE_SIZE - 1); rows = result.data ?? []; fetchError = result.error; }
    if (fetchError) { setError(fetchError.message); setLoading(false); setLoadingMore(false); return; }
    const ownerIds = Array.from(new Set(rows.map(item => item.user_id))); const badgeResult = ownerIds.length ? await supabase.from("bridgex_member_badges").select("id,display_name,is_verified").in("id", ownerIds) : { data: [] }; const badgeById = new Map((badgeResult.data ?? []).map((badge: any) => [badge.id, badge]));
    const rowsWithImages = await addImages(rows, item => view === "requests" ? item.image_path ?? (Array.isArray(item.media_paths) ? item.media_paths[0] : null) : (Array.isArray(item.media_paths) ? item.media_paths[0] : null));
    const mapped: MarketItem[] = rowsWithImages.map((item: any) => { const badge: any = badgeById.get(item.user_id); return view === "requests" ? { id: item.id, ownerId: item.user_id, title: item.title, category: item.category, productCategories: Array.isArray(item.categories) ? item.categories : [], route: `${item.purchase_country} → ${item.destination_country ?? item.destination_district}`, location: item.destination_city, weight: `${item.weight_kg} kg`, budget: `৳ ${Number(item.budget_bdt).toLocaleString()}`, date: "Open now", tag: "Item request", imageUrl: item.imageUrl, posterName: badge?.display_name || "BridgeX member", posterVerified: Boolean(badge?.is_verified) } : { id: item.id, ownerId: item.user_id, title: `${item.transport_mode === "cargo" ? "Cargo" : "Carry"} space available`, category: item.transport_mode, productCategories: [], route: `${item.origin_city}, ${item.origin_country} → ${item.destination_city}, ${item.destination_country ?? item.destination_district}`, location: item.destination_city, weight: `${item.available_weight_kg} kg available`, budget: `৳ ${Number(item.price_bdt).toLocaleString()} / ${item.pricing_mode === "per_kg" ? "kg" : "item"}`, date: new Date(item.departure_at).toLocaleDateString(), tag: item.transport_mode === "cargo" ? "Cargo" : "Carry space", imageUrl: item.imageUrl, posterName: badge?.display_name || "BridgeX member", posterVerified: Boolean(badge?.is_verified) }; });
    setItems(previous => reset ? mapped : [...previous, ...mapped]); setPage(nextPage + 1); setHasMore(rows.length === PAGE_SIZE); setLoading(false); setLoadingMore(false);
  };
  useEffect(() => { setCategory("All categories"); setPage(0); void load(true); }, [view]); useEffect(() => () => clearObjectUrls(), []);
  const categories = useMemo(() => ["All categories", ...Array.from(new Set(items.flatMap(item => [item.category, ...item.productCategories]).filter(Boolean))).sort()], [items]); const filtered = useMemo(() => items.filter(item => (category === "All categories" || item.category === category || item.productCategories.includes(category)) && `${item.title} ${item.route} ${item.location} ${item.category} ${item.productCategories.join(" ")}`.toLowerCase().includes(query.toLowerCase())), [items, query, category]); const toggleSaved = (id: string) => setSaved(previous => { const next = new Set(previous); next.has(id) ? next.delete(id) : next.add(id); return next; });
  return <PublicLayout><main className="px-5 py-10 lg:px-8 lg:py-14"><div className="mx-auto max-w-[1344px]"><div className="flex flex-col justify-between gap-6 lg:flex-row lg:items-end"><div><p className="text-xs font-bold uppercase tracking-[0.18em] text-[#2d8d62]">Global goods-carrying marketplace</p><h1 className="mt-3 font-display text-4xl font-bold tracking-[-0.055em] sm:text-5xl">Find a trusted person to carry your goods.</h1><p className="mt-3 max-w-xl text-sm leading-6 text-[#637073]">Explore live requests and carry capacity from anywhere. Posts are paginated to keep the marketplace responsive as activity grows.</p></div><div className="rounded-xl bg-[#e9e5d9] p-1"><button onClick={() => setView("requests")} className={`rounded-lg px-4 py-2 text-sm font-bold ${view === "requests" ? "bg-[#172126] text-white shadow-sm" : "text-[#596466]"}`}>Item requests</button><button onClick={() => setView("flights")} className={`rounded-lg px-4 py-2 text-sm font-bold ${view === "flights" ? "bg-[#172126] text-white shadow-sm" : "text-[#596466]"}`}>Carry space</button></div></div><div className="mt-9 grid gap-3 md:grid-cols-[1fr_240px]"><div className="flex h-11 items-center gap-2 rounded-xl border border-[#172126]/8 bg-white px-3"><Search className="size-4 text-[#647174]" /><Input value={query} onChange={event => setQuery(event.target.value)} placeholder="Search product, origin, destination, or route" className="h-auto border-0 bg-transparent p-0 text-sm shadow-none focus-visible:ring-0" /></div><select value={category} onChange={event => setCategory(event.target.value)} className="h-11 rounded-xl border border-[#172126]/8 bg-white px-3 text-sm font-semibold text-[#4e5b5d]"><option value="All categories">All categories</option>{categories.filter(item => item !== "All categories").map(item => <option key={item}>{item}</option>)}</select></div><div className="mt-8 flex items-center justify-between"><p className="text-sm font-semibold text-[#526063]">{loading ? "Loading live posts…" : `${filtered.length} ${view === "requests" ? "requests" : "listings"} shown`}</p><Button onClick={() => void load(true)} variant="outline" className="rounded-xl bg-white">Refresh posts</Button></div>{error && <p className="mt-4 rounded-xl bg-[#f8e8e5] px-3 py-2 text-sm font-semibold text-[#9b4b3e]">{error}</p>}<div className="mt-5 grid gap-3 sm:grid-cols-2 xl:grid-cols-3">{filtered.map(item => { const ownPost = item.ownerId === user?.id; const detailHref = `/post/${view === "requests" ? "request" : "listing"}/${item.id}`; return <article key={item.id} className="group overflow-hidden rounded-2xl border border-[#172126]/8 bg-white transition hover:border-[#2d8d62]/35 hover:shadow-[0_12px_24px_rgba(32,45,41,0.08)]">{item.imageUrl ? <img src={item.imageUrl} alt={`Photo for ${item.title}`} className="h-32 w-full object-cover" loading="lazy" /> : <div className="grid h-20 place-items-center bg-[#f1f5f0]"><PackageCheck className="size-5 text-[#86a093]" /></div>}<div className="p-4"><div className="flex items-start justify-between gap-2"><Badge className="rounded-full bg-[#e7f4ea] px-2.5 py-1 text-[10px] font-bold uppercase tracking-[0.08em] text-[#23784f] hover:bg-[#e7f4ea]">{item.tag}</Badge><button onClick={() => toggleSaved(item.id)} aria-label="Save listing" className={`grid size-7 place-items-center rounded-full ${saved.has(item.id) ? "bg-[#fdf0d0] text-[#ad7420]" : "text-[#687476]"}`}><Heart className={`size-4 ${saved.has(item.id) ? "fill-current" : ""}`} /></button></div><Link href={detailHref} className="mt-4 block text-base font-bold tracking-[-0.02em] hover:text-[#176447]">{item.title}</Link>{item.productCategories.length > 0 && <p className="mt-1 truncate text-xs font-semibold text-[#87600b]">{item.productCategories.join(" · ")}</p>}<div className="mt-3 grid gap-1.5 text-xs text-[#5d696c]"><p className="flex items-center gap-1.5"><Plane className="size-3.5 text-[#2d8d62]" />{item.route}</p><p className="flex items-center gap-1.5"><MapPin className="size-3.5 text-[#2d8d62]" />{item.location}</p><p className="flex items-center gap-1.5"><Weight className="size-3.5 text-[#2d8d62]" />{item.weight}</p><p className="flex items-center gap-1.5"><CalendarDays className="size-3.5 text-[#2d8d62]" />{item.date}</p></div><div className="mt-4 flex items-center justify-between border-t border-[#172126]/7 pt-3"><div><p className="text-[10px] font-semibold text-[#788481]">{view === "requests" ? "Budget" : "Price"}</p><p className="mt-0.5 text-sm font-bold">{item.budget}</p></div><span className="text-xs font-bold text-[#637073]">Live post</span></div><div className="mt-3 flex items-center justify-between gap-2"><span className="min-w-0"><span className="block truncate text-xs font-bold">{item.posterName}</span><span className="mt-1 inline-flex"><MemberVerificationBadge verified={item.posterVerified} /></span></span><Link href={detailHref}><Button size="sm" variant="outline" className="h-9 rounded-lg bg-[#f9f8f5] text-xs font-bold">{ownPost ? (view === "requests" ? "Offers" : "Interest") : "Details"}</Button></Link></div></div></article>; })}</div>{!loading && filtered.length === 0 && <div className="mt-8 rounded-3xl border border-dashed border-[#172126]/18 bg-white p-12 text-center"><PackageCheck className="mx-auto size-8 text-[#2d8d62]" /><h2 className="mt-4 font-bold">No live posts found</h2><p className="mt-2 text-sm text-[#647174]">Be the first to publish a request or list your carry space.</p></div>}{hasMore && <div className="mt-8 text-center"><Button disabled={loadingMore} onClick={() => void load(false)} variant="outline" className="rounded-xl bg-white">{loadingMore ? "Loading posts…" : "Load more posts"}</Button></div>}</div></main></PublicLayout>;
}
