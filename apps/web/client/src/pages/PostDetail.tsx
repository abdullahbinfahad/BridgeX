import { useAuth } from "@/_core/hooks/useAuth";
import PublicLayout from "@/components/bridgex/PublicLayout";
import { MemberVerificationBadge } from "@/components/bridgex/VerifiedBadge";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { supabase } from "@/lib/supabase";
import { ArrowLeft, CalendarDays, ImageIcon, MapPin, MessageSquareMore, PackageCheck, Plane, ShieldCheck, UserRound, Weight } from "lucide-react";
import { useEffect, useState } from "react";
import { Link, useRoute } from "wouter";

type PostKind = "request" | "listing";
type PostRecord = Record<string, any> & { id: string; user_id: string; status: string; media_paths?: string[] | null; image_path?: string | null };
type Poster = { display_name: string; is_verified: boolean };
function money(value: unknown) { return `৳ ${Number(value ?? 0).toLocaleString()}`; }
function postMediaPaths(post: PostRecord) { return Array.from(new Set([...(Array.isArray(post.media_paths) ? post.media_paths : []), ...(post.image_path ? [post.image_path] : [])])); }

export default function PostDetail() {
  const [, params] = useRoute("/post/:kind/:id"); const kind: PostKind = params?.kind === "listing" ? "listing" : "request"; const postId = params?.id; const { user, isAuthenticated } = useAuth();
  const [post, setPost] = useState<PostRecord | null>(null); const [poster, setPoster] = useState<Poster | null>(null); const [mediaUrls, setMediaUrls] = useState<string[]>([]); const [responseCount, setResponseCount] = useState(0); const [loading, setLoading] = useState(true); const [error, setError] = useState("");
  useEffect(() => { if (!postId) return; let active = true; const urls: string[] = []; void (async () => { setLoading(true); setError(""); const table = kind === "request" ? "send_requests" : "carry_listings"; const { data, error: loadError } = await supabase.from(table).select("*").eq("id", postId).maybeSingle(); if (!active) return; if (loadError || !data) { setError(loadError?.message ?? "This post is unavailable or no longer open."); setLoading(false); return; } const record = data as PostRecord; setPost(record); const [badgeResult, responseResult] = await Promise.all([supabase.from("bridgex_member_badges").select("display_name,is_verified").eq("id", record.user_id).maybeSingle(), supabase.from(kind === "request" ? "offers" : "listing_interests").select("id", { count: "exact", head: true }).eq(kind === "request" ? "request_id" : "listing_id", postId)]); if (active) { setPoster(badgeResult.data as Poster | null); setResponseCount(responseResult.count ?? 0); } const paths = postMediaPaths(record); const downloads = await Promise.all(paths.map(async path => { const { data: file } = await supabase.storage.from("request-media").download(path); if (!file) return null; const url = URL.createObjectURL(file); urls.push(url); return url; })); if (active) setMediaUrls(downloads.filter((url): url is string => Boolean(url))); setLoading(false); })(); return () => { active = false; urls.forEach(URL.revokeObjectURL); }; }, [kind, postId]);
  if (loading) return <PublicLayout><main className="mx-auto max-w-5xl px-5 py-14 text-sm font-semibold text-[#647174]">Loading this live post…</main></PublicLayout>;
  if (error || !post) return <PublicLayout><main className="mx-auto max-w-5xl px-5 py-14"><Link href="/marketplace" className="inline-flex items-center gap-2 text-sm font-bold text-[#176447]"><ArrowLeft className="size-4" />Marketplace</Link><div className="mt-6 rounded-3xl bg-white p-8"><h1 className="font-display text-3xl font-bold">Post unavailable</h1><p className="mt-3 text-sm text-[#647174]">{error || "This post cannot be opened."}</p></div></main></PublicLayout>;
  const ownPost = user?.id === post.user_id; const request = kind === "request"; const title = request ? post.title : `${post.transport_mode === "cargo" ? "Cargo" : "Carry"} space available`; const route = request ? `${post.purchase_country} → ${post.destination_country ?? post.destination_district}, ${post.destination_city}` : `${post.origin_city}, ${post.origin_country} → ${post.destination_city}, ${post.destination_country ?? post.destination_district}`; const detail = request ? `${post.weight_kg} kg · Budget ${money(post.budget_bdt)}` : `${post.available_weight_kg} kg available · ${money(post.price_bdt)} / ${post.pricing_mode === "per_kg" ? "kg" : "item"}`; const actionHref = request ? `/offer?request=${post.id}` : `/interest?listing=${post.id}`;
  return <PublicLayout><main className="px-5 py-10 lg:px-8 lg:py-14"><div className="mx-auto max-w-5xl"><Link href="/marketplace" className="inline-flex items-center gap-2 text-sm font-bold text-[#176447]"><ArrowLeft className="size-4" />Back to marketplace</Link><div className="mt-7 grid gap-7 lg:grid-cols-[1.15fr_0.85fr]"><section><div className="flex flex-wrap items-center gap-2"><Badge className={request ? "bg-[#e7f4ea] text-[#176447]" : "bg-[#172126] text-white"}>{request ? "Item request" : "Carry space"}</Badge><span className="rounded-full bg-[#f0eee7] px-2.5 py-1 text-xs font-bold text-[#607073]">{post.status}</span></div><h1 className="mt-4 font-display text-4xl font-bold tracking-[-0.055em] sm:text-5xl">{title}</h1><div className="mt-4 flex flex-wrap items-center gap-2 rounded-2xl bg-[#f4faf5] px-3 py-2"><UserRound className="size-4 text-[#2d8d62]" /><span className="text-sm font-bold">Posted by {poster?.display_name || "BridgeX member"}</span><MemberVerificationBadge verified={Boolean(poster?.is_verified)} /></div><div className="mt-6 grid gap-3 text-sm leading-6 text-[#536164]"><p className="flex gap-2"><Plane className="mt-0.5 size-4 shrink-0 text-[#2d8d62]" />{route}</p><p className="flex gap-2"><Weight className="mt-0.5 size-4 shrink-0 text-[#2d8d62]" />{detail}</p><p className="flex gap-2"><CalendarDays className="mt-0.5 size-4 shrink-0 text-[#2d8d62]" />Published {new Date(post.created_at).toLocaleString()}</p>{post.destination_address && <p className="flex gap-2"><MapPin className="mt-0.5 size-4 shrink-0 text-[#2d8d62]" />Exact address is protected until a trusted match is selected.</p>}</div><div className="mt-7 rounded-3xl border border-[#172126]/8 bg-white p-6"><h2 className="text-lg font-bold">Post details</h2><p className="mt-3 whitespace-pre-wrap text-sm leading-7 text-[#526063]">{request ? post.description : post.notes || "No additional notes were provided."}</p>{request && post.size_description && <p className="mt-4 text-sm"><strong>Size / quantity:</strong> {post.size_description}</p>}{request && post.product_link && <a href={post.product_link} target="_blank" rel="noreferrer" className="mt-4 inline-flex text-sm font-bold text-[#176447]">Open product link</a>}</div></section><aside className="space-y-5"><div className="rounded-3xl bg-[#172126] p-6 text-[#f7f5ef]"><ShieldCheck className="size-5 text-[#91e7bc]" />{ownPost ? <><h2 className="mt-5 text-xl font-bold">Your live post</h2><p className="mt-2 text-sm leading-6 text-[#c3d0c9]">{responseCount} {request ? "offer" : "interest"}{responseCount === 1 ? " has" : "s have"} reached this post.</p><Link href="/dashboard/offers"><Button className="mt-5 w-full rounded-xl bg-[#91e7bc] font-bold text-[#172126] hover:bg-[#b7efcc]"><MessageSquareMore className="mr-2 size-4" />{request ? "View offers" : "View interested members"}</Button></Link></> : <><h2 className="mt-5 text-xl font-bold">Ready to respond?</h2><p className="mt-2 text-sm leading-6 text-[#c3d0c9]">{request ? "Make a clear offer with your price and delivery date." : "Tell this traveler what you need carried and why the route fits."}</p><Link href={isAuthenticated ? actionHref : "/access"}><Button className="mt-5 w-full rounded-xl bg-[#91e7bc] font-bold text-[#172126] hover:bg-[#b7efcc]">{request ? "Make an offer" : "Show interest"}</Button></Link></>}</div><div className="rounded-3xl border border-[#172126]/8 bg-white p-6"><div className="flex items-center gap-2"><PackageCheck className="size-5 text-[#2d8d62]" /><h2 className="font-bold">Safer matching</h2></div><p className="mt-3 text-sm leading-6 text-[#637073]">Review the details, compare responses, and use the protected order process only after both members agree.</p></div></aside></div>{mediaUrls.length > 0 && <section className="mt-8"><div className="flex items-center gap-2"><ImageIcon className="size-5 text-[#2d8d62]" /><h2 className="text-xl font-bold">Photos and video</h2></div><div className="mt-4 grid gap-4 sm:grid-cols-2">{mediaUrls.map((url, index) => <div key={url} className="overflow-hidden rounded-3xl border border-[#172126]/8 bg-black">{postMediaPaths(post)[index]?.match(/\.(mp4|webm|mov)$/i) ? <video src={url} controls className="max-h-[440px] w-full" /> : <img src={url} alt={`Media ${index + 1} for ${title}`} className="max-h-[440px] w-full object-cover" />}</div>)}</div></section>}</div></main></PublicLayout>;
}
