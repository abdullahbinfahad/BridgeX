import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { supabase } from "@/lib/supabase";
import { ArrowLeft, Mail, RefreshCw, Search } from "lucide-react";
import { useEffect, useMemo, useState } from "react";
import { useLocation } from "wouter";
type Enquiry = { id: string; user_id: string | null; name: string; email: string; subject: string; message: string; reply_body: string | null; status: "open" | "in_review" | "resolved"; created_at: string };
export default function AdminEnquiries() { const [, go] = useLocation(); const [rows, setRows] = useState<Enquiry[]>([]); const [query, setQuery] = useState(""); const [notice, setNotice] = useState(""); const [loading, setLoading] = useState(true); const load = async () => { setLoading(true); const { data, error } = await supabase.from("contact_enquiries").select("id,user_id,name,email,subject,message,reply_body,status,created_at").order("created_at", { ascending: false }).limit(500); setLoading(false); if (error) setNotice(error.message); else setRows((data ?? []) as Enquiry[]); }; useEffect(() => { void load(); }, []); const visible = useMemo(() => rows.filter(row => `${row.name} ${row.email} ${row.subject} ${row.message} ${row.status}`.toLowerCase().includes(query.toLowerCase())), [rows, query]); const reply = async (row: Enquiry) => { const body = window.prompt("Reply as BridgeX Admin:", row.reply_body ?? ""); if (!body?.trim()) return; const { error } = await supabase.from("contact_enquiries").update({ reply_body: body.trim(), replied_at: new Date().toISOString(), status: "resolved", resolved_at: new Date().toISOString() }).eq("id", row.id); if (error) return setNotice(error.message); if (row.user_id) await supabase.from("notifications").insert({ user_id: row.user_id, type: "contact_reply", title: "BridgeX Admin", body: body.trim(), link: "/dashboard/deals", related_id: row.id }); setNotice("Reply sent as BridgeX Admin and the enquiry was resolved."); await load(); }; return <main className="min-h-screen bg-[#f7f5ef] px-5 py-8 text-[#172126]"><div className="mx-auto max-w-5xl"><Button onClick={() => go("/admin")} variant="outline" className="rounded-xl bg-white"><ArrowLeft className="mr-2 size-4" />Control panel</Button><div className="mt-7 flex flex-wrap items-end justify-between gap-4"><div><p className="text-xs font-bold uppercase tracking-[0.16em] text-[#2d8d62]">Administrator support</p><h1 className="mt-2 font-display text-4xl font-bold">Contact enquiries</h1></div><Button onClick={() => void load()} variant="outline" className="rounded-xl bg-white"><RefreshCw className="mr-2 size-4" />Refresh</Button></div><div className="mt-6 flex h-11 items-center gap-2 rounded-xl border border-[#172126]/10 bg-white px-3"><Search className="size-4 text-[#647174]" /><Input value={query} onChange={event => setQuery(event.target.value)} placeholder="Search enquiries" className="h-auto border-0 bg-transparent p-0 shadow-none focus-visible:ring-0" /></div>{notice && <p className="mt-4 rounded-xl bg-[#edf8f0] px-3 py-2 text-sm font-semibold text-[#176447]">{notice}</p>}<div className="mt-6 grid gap-4">{loading ? <p className="rounded-2xl bg-white p-6">Loading enquiries…</p> : visible.map(row => <article key={row.id} className="rounded-3xl border border-[#172126]/8 bg-white p-6"><div className="flex flex-wrap items-start justify-between gap-3"><div><p className="font-bold"><Mail className="mr-2 inline size-4 text-[#2d8d62]" />{row.subject}</p><p className="mt-2 text-sm">{row.name} · <a href={`mailto:${row.email}`} className="text-[#176447]">{row.email}</a></p></div><span className="rounded-full bg-[#e7f4ea] px-2.5 py-1 text-xs font-bold text-[#176447]">{row.status}</span></div><p className="mt-4 whitespace-pre-wrap text-sm leading-6 text-[#526063]">{row.message}</p>{row.reply_body && <div className="mt-4 rounded-2xl bg-[#f4faf5] p-4 text-sm"><p className="font-bold text-[#176447]">BridgeX Admin</p><p className="mt-2 whitespace-pre-wrap leading-6">{row.reply_body}</p></div>}<div className="mt-5 flex gap-2"><Button onClick={() => void reply(row)} size="sm" className="rounded-lg bg-[#176447]">{row.reply_body ? "Update reply" : "Reply to user"}</Button></div></article>)}</div></div></main>; }
