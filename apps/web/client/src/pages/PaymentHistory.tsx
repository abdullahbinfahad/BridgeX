import { useAuth } from "@/_core/hooks/useAuth";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Textarea } from "@/components/ui/textarea";
import { compressImageForUpload } from "@/lib/fileUpload";
import { supabase } from "@/lib/supabase";
import { CheckCircle2, CreditCard, FileUp, ImagePlus, Landmark, Loader2, WalletCards } from "lucide-react";
import { useEffect, useMemo, useState } from "react";
import { Link } from "wouter";

type PaymentProof = { id: string; reference: string; response_kind: "offer" | "interest"; amount: number; currency: string; status: string; payment_method: "alipay" | "wechat_pay" | null; payer_reference: string | null; payer_note: string | null; submitted_at: string | null; reviewer_note: string | null; created_at: string };
type TravelerPayout = { id: string; order_id: string; amount: number; currency: string; payout_status: "details_required" | "payment_due" | "payment_sent" | "received"; payout_method: "alipay" | "wechat_pay" | "bank_transfer" | null; account_holder: string | null; account_reference: string | null; qr_path: string | null; payment_reference: string | null; administrator_note: string | null; paid_at: string | null; received_at: string | null; created_at: string };
type PayoutProfile = { payout_method: "alipay" | "wechat_pay" | "bank_transfer"; account_holder: string; account_reference: string | null; qr_path: string | null };

const money = (amount: number, currency: string) => new Intl.NumberFormat(undefined, { style: "currency", currency: currency || "BDT", maximumFractionDigits: 2 }).format(Number(amount || 0));
const label = (value: string) => value.replace(/_/g, " ").replace(/\b\w/g, character => character.toUpperCase());
const tone = (status: string) => status === "verified" || status === "received" ? "bg-[#dff5ea] text-[#176447]" : status === "payment_verifying" || status === "payment_sent" ? "bg-[#fff1ce] text-[#805700]" : status === "payment_due" ? "bg-[#eef6ff] text-[#205d8e]" : "bg-[#eef1f1] text-[#536164]";
const nativeInput = "h-11 w-full rounded-xl border border-[#d9d7cf] bg-white px-3 text-sm outline-none focus:border-[#2d8d62] focus:ring-2 focus:ring-[#2d8d62]/20";

export default function PaymentHistory({ mode = "payments" }: { mode?: "payments" | "payouts" }) {
  const { user } = useAuth();
  const query = new URLSearchParams(window.location.search);
  const statusView = query.get("status") ?? "";
  const paymentParam = query.get("payment") ?? "";
  const [payments, setPayments] = useState<PaymentProof[]>([]); const [payouts, setPayouts] = useState<TravelerPayout[]>([]); const [profile, setProfile] = useState<PayoutProfile | null>(null); const [loading, setLoading] = useState(true); const [notice, setNotice] = useState(""); const [selectedId, setSelectedId] = useState(""); const [proofFile, setProofFile] = useState<File | null>(null); const [method, setMethod] = useState<"alipay" | "wechat_pay">("alipay"); const [reference, setReference] = useState(""); const [note, setNote] = useState(""); const [submitting, setSubmitting] = useState(false); const [payoutMethod, setPayoutMethod] = useState<PayoutProfile["payout_method"]>("alipay"); const [accountHolder, setAccountHolder] = useState(""); const [accountReference, setAccountReference] = useState(""); const [payoutQr, setPayoutQr] = useState<File | null>(null); const [savingPayout, setSavingPayout] = useState(false); const [confirmingPayoutId, setConfirmingPayoutId] = useState("");
  const selected = payments.find(item => item.id === selectedId || item.id === paymentParam) ?? payments.find(item => ["pending_payment", "rejected"].includes(item.status)) ?? payments[0] ?? null;
  const counts = useMemo(() => ({ pending: payments.filter(item => ["pending_payment", "rejected"].includes(item.status)).length, verifying: payments.filter(item => item.status === "payment_verifying").length, verified: payments.filter(item => item.status === "verified").length, received: payouts.filter(item => item.payout_status === "received").length }), [payments, payouts]);
  const statusCards = [["pending", "Pending payment", counts.pending, "Payment record needs attention"], ["verifying", "Payment verifying", counts.verifying, "Review is in progress"], ["verified", "Payment verified", counts.verified, "Protected matches opened"], ["received", "Payment received", counts.received, "Traveler payouts confirmed"]] as const;

  const load = async () => { if (!user) return; setLoading(true); const [paymentResult, payoutResult, profileResult] = await Promise.all([supabase.from("bridgex_payment_proofs").select("id,reference,response_kind,amount,currency,status,payment_method,payer_reference,payer_note,submitted_at,reviewer_note,created_at").eq("payer_id", user.id).order("created_at", { ascending: false }).limit(100), supabase.from("bridgex_traveler_payouts").select("id,order_id,amount,currency,payout_status,payout_method,account_holder,account_reference,qr_path,payment_reference,administrator_note,paid_at,received_at,created_at").eq("traveler_id", user.id).order("created_at", { ascending: false }).limit(100), supabase.from("bridgex_traveler_payout_profiles").select("payout_method,account_holder,account_reference,qr_path").eq("traveler_id", user.id).maybeSingle()]); setPayments((paymentResult.data ?? []) as PaymentProof[]); setPayouts((payoutResult.data ?? []) as TravelerPayout[]); const next = profileResult.data as PayoutProfile | null; setProfile(next); if (next) { setPayoutMethod(next.payout_method); setAccountHolder(next.account_holder); setAccountReference(next.account_reference ?? ""); } setLoading(false); const error = paymentResult.error?.message || payoutResult.error?.message || profileResult.error?.message; if (error) setNotice(error); };
  useEffect(() => { void load(); }, [user?.id]);

  const submitProof = async () => { if (!user || !selected || !proofFile) return setNotice("Choose a clear payment screenshot before submitting."); setSubmitting(true); setNotice(""); try { const file = await compressImageForUpload(proofFile); const path = `${user.id}/${selected.id}/${crypto.randomUUID()}-${file.name.replace(/[^a-zA-Z0-9._-]/g, "_")}`; const upload = await supabase.storage.from("payment-proofs").upload(path, file, { contentType: file.type, upsert: false }); if (upload.error) return setNotice(upload.error.message); const { error } = await supabase.rpc("submit_bridgex_payment_proof", { p_payment_id: selected.id, p_payment_method: method, p_proof_path: path, p_payer_reference: reference.trim() || null, p_payer_note: note.trim() || null }); if (error) return setNotice(error.message); setProofFile(null); setReference(""); setNote(""); setNotice("Payment evidence submitted."); await load(); } finally { setSubmitting(false); } };
  const savePayoutDetails = async () => { if (!user || !accountHolder.trim()) return setNotice("Add the account holder name."); if (payoutMethod === "bank_transfer" && !accountReference.trim()) return setNotice("Add bank account or routing details."); setSavingPayout(true); setNotice(""); try { let qrPath = profile?.qr_path ?? null; if (payoutQr) { const file = await compressImageForUpload(payoutQr); qrPath = `${user.id}/${crypto.randomUUID()}-${file.name.replace(/[^a-zA-Z0-9._-]/g, "_")}`; const upload = await supabase.storage.from("traveler-payout-instructions").upload(qrPath, file, { contentType: file.type, upsert: false }); if (upload.error) return setNotice(upload.error.message); } const { error } = await supabase.rpc("save_bridgex_traveler_payout_profile", { p_method: payoutMethod, p_account_holder: accountHolder.trim(), p_account_reference: accountReference.trim() || null, p_qr_path: qrPath }); if (error) return setNotice(error.message); setPayoutQr(null); setNotice("Private payout details saved."); await load(); } finally { setSavingPayout(false); } };
  const confirmReceived = async (payout: TravelerPayout) => { if (confirmingPayoutId !== payout.id) return setConfirmingPayoutId(payout.id); const { error } = await supabase.rpc("confirm_bridgex_traveler_payout_received", { p_payout_id: payout.id }); if (error) return setNotice(error.message); setConfirmingPayoutId(""); setNotice("Payment receipt confirmed."); await load(); };
  const message = notice && <p className={`mt-5 rounded-xl p-3 text-sm font-semibold ${/saved|submitted|confirmed/.test(notice) ? "bg-[#dff5ea] text-[#176447]" : "bg-[#f8e8e5] text-[#9b4b3e]"}`}>{notice}</p>;
  const PayoutSetup = () => <section className="rounded-3xl border border-[#172126]/8 bg-white p-6"><div className="flex items-start justify-between gap-4"><div><p className="text-xs font-bold uppercase tracking-[0.14em] text-[#2d8d62]">Traveler payout details</p><h2 className="mt-1 text-xl font-bold">Private payout QR or bank details</h2><p className="mt-2 max-w-2xl text-sm leading-6 text-[#637073]">Save reusable private payout details for eligible completed orders.</p></div><Landmark className="size-6 text-[#176447]" /></div><div className="mt-5 grid gap-4 md:grid-cols-2"><label className="grid gap-2 text-sm font-bold">Payout method<select value={payoutMethod} onChange={event => setPayoutMethod(event.target.value as PayoutProfile["payout_method"])} className={nativeInput}><option value="alipay">Alipay</option><option value="wechat_pay">WeChat Pay</option><option value="bank_transfer">Bank transfer</option></select></label><label className="grid gap-2 text-sm font-bold">Account holder name<input value={accountHolder} onChange={event => setAccountHolder(event.target.value)} autoComplete="name" className={nativeInput} placeholder="Name used by your payout account" /></label><label className="grid gap-2 text-sm font-bold md:col-span-2">{payoutMethod === "bank_transfer" ? "Bank account / routing details" : "Payout ID or account reference (optional)"}<input value={accountReference} onChange={event => setAccountReference(event.target.value)} autoComplete="off" className={nativeInput} placeholder={payoutMethod === "bank_transfer" ? "Bank name, account number, routing details" : "Wallet ID, phone, or account reference"} /></label>{payoutMethod !== "bank_transfer" && <label className="grid gap-2 text-sm font-bold md:col-span-2">Private payout QR image (optional)<span className="flex h-11 cursor-pointer items-center rounded-xl border border-[#172126]/12 bg-[#fbfaf7] px-3 text-sm"><ImagePlus className="mr-2 size-4" />{payoutQr?.name || (profile?.qr_path ? "Private QR saved — choose to replace" : "Choose your QR image")}<input className="sr-only" type="file" accept="image/jpeg,image/png,image/webp" onChange={event => setPayoutQr(event.target.files?.[0] ?? null)} /></span></label>}</div><Button onClick={() => void savePayoutDetails()} disabled={savingPayout} className="mt-5 rounded-xl bg-[#172126] font-bold">{savingPayout ? <><Loader2 className="mr-2 size-4 animate-spin" />Saving…</> : "Save private payout details"}</Button></section>;
  const PayoutRecords = () => <section className="rounded-3xl border border-[#172126]/8 bg-white p-6"><div className="flex items-center gap-2"><CreditCard className="size-5 text-[#176447]" /><div><p className="text-xs font-bold uppercase tracking-[0.14em] text-[#2d8d62]">Traveler payouts</p><h2 className="mt-1 font-bold">Traveler payout history</h2></div></div><div className="mt-5 grid gap-3">{loading ? <p className="rounded-xl bg-[#f5f3ed] p-4 text-sm text-[#637073]">Loading traveler payouts…</p> : payouts.length ? payouts.map(payout => <article key={payout.id} className="flex flex-wrap items-start justify-between gap-4 rounded-2xl bg-[#f6f4ee] p-4"><div><p className="font-bold">{money(payout.amount, payout.currency)}</p><p className="mt-1 text-sm text-[#637073]">{payout.payout_method ? label(payout.payout_method) : "Payout details required"} · Released {new Date(payout.created_at).toLocaleDateString()}</p>{payout.payment_reference && <p className="mt-1 text-xs text-[#687579]">Reference: {payout.payment_reference}</p>}</div><div className="flex items-center gap-2"><Badge className={tone(payout.payout_status)}>{label(payout.payout_status)}</Badge>{payout.payout_status === "payment_sent" && <Button onClick={() => void confirmReceived(payout)} size="sm" className="rounded-lg bg-[#176447]"><CheckCircle2 className="mr-1.5 size-4" />{confirmingPayoutId === payout.id ? "Confirm now" : "Confirm received"}</Button>}</div></article>) : <p className="rounded-xl bg-[#f5f3ed] p-4 text-sm text-[#637073]">Traveler payouts appear here after completed orders are released.</p>}</div></section>;

  if (mode === "payouts") return <div><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="text-xs font-bold uppercase tracking-[0.16em] text-[#2d8d62]">Workspace</p><h1 className="mt-2 font-display text-4xl font-bold tracking-[-0.055em]">Traveler payouts</h1><p className="mt-2 text-sm text-[#637073]">Your completed-order payout records and private payout setup.</p></div><Link href="/dashboard/payments"><Button className="rounded-xl bg-[#172126] font-bold">Payments</Button></Link></div>{message}<div className="mt-7 grid gap-7"><PayoutRecords /><PayoutSetup /></div></div>;
  if (["pending", "verifying", "verified", "received"].includes(statusView)) { const card = statusCards.find(item => item[0] === statusView)!; const filteredPayments = statusView === "pending" ? payments.filter(item => ["pending_payment", "rejected"].includes(item.status)) : statusView === "verifying" ? payments.filter(item => item.status === "payment_verifying") : statusView === "verified" ? payments.filter(item => item.status === "verified") : []; const filteredPayouts = statusView === "received" ? payouts.filter(item => item.payout_status === "received") : []; return <div><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="text-xs font-bold uppercase tracking-[0.16em] text-[#2d8d62]">Payments</p><h1 className="mt-2 font-display text-4xl font-bold tracking-[-0.055em]">{card[1]}</h1><p className="mt-2 text-sm text-[#637073]">{card[3]}</p></div><Link href="/dashboard/payments"><Button className="rounded-xl bg-[#172126] font-bold">Back to Payments</Button></Link></div><section className="mt-7 rounded-3xl border border-[#172126]/8 bg-white p-5"><div className="grid gap-3">{loading ? <p className="rounded-xl bg-[#f5f3ed] p-4 text-sm text-[#637073]">Loading records…</p> : statusView === "received" ? filteredPayouts.length ? filteredPayouts.map(payout => <article key={payout.id} className="rounded-2xl bg-[#f6f4ee] p-4"><p className="font-bold">{money(payout.amount, payout.currency)}</p><p className="mt-1 text-sm text-[#637073]">Traveler payout · {new Date(payout.received_at ?? payout.created_at).toLocaleDateString()}</p></article>) : <p className="rounded-xl bg-[#f5f3ed] p-4 text-sm text-[#637073]">No confirmed traveler payouts yet.</p> : filteredPayments.length ? filteredPayments.map(payment => <Link key={payment.id} href={`/dashboard/payments?payment=${payment.id}`} className="rounded-2xl border border-[#172126]/8 bg-[#fbfaf7] p-4"><div className="flex items-center justify-between"><strong>{payment.reference}</strong><Badge className={tone(payment.status)}>{label(payment.status)}</Badge></div><p className="mt-2 text-sm font-semibold">{money(payment.amount, payment.currency)}</p></Link>) : <p className="rounded-xl bg-[#f5f3ed] p-4 text-sm text-[#637073]">No records in this status.</p>}</div></section></div>; }
  return <div><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="text-xs font-bold uppercase tracking-[0.16em] text-[#2d8d62]">Workspace</p><h1 className="mt-2 font-display text-4xl font-bold tracking-[-0.055em]">Payments</h1><p className="mt-2 max-w-2xl text-sm leading-6 text-[#637073]">Your newest 10 payment records, current protected service status, and traveler payout access.</p><p className="mt-3 text-xs leading-5 text-[#687579]">A 5% BridgeX platform service fee applies to completed transactions. Any applicable amount appears only in the protected payment record.</p></div><div className="flex gap-2"><Link href="/dashboard/payouts"><Button variant="outline" className="rounded-xl bg-white">Traveler payouts</Button></Link><Link href="/dashboard/offers"><Button className="rounded-xl bg-[#172126] font-bold">Incoming offers</Button></Link></div></div><div className="mt-7 grid gap-3 sm:grid-cols-2 xl:grid-cols-4">{statusCards.map(([key, title, count, copy]) => <Link key={key} href={`/dashboard/payments?status=${key}`} className="rounded-2xl border border-[#172126]/8 bg-white p-4 transition hover:border-[#2d8d62]/45 hover:bg-[#f4faf6]"><p className="text-xs font-bold uppercase tracking-[0.1em] text-[#637073]">{title}</p><p className="mt-4 font-display text-3xl font-bold">{count}</p><p className="mt-1 text-xs text-[#687579]">{copy}</p></Link>)}</div>{message}<div className="mt-7 grid gap-6 xl:grid-cols-[0.86fr_1.14fr]"><section className="rounded-3xl border border-[#172126]/8 bg-white p-5"><div className="flex items-center gap-2"><WalletCards className="size-5 text-[#176447]" /><div><h2 className="font-bold">Your payment records</h2><p className="mt-1 text-sm text-[#637073]">Newest 10 records.</p></div></div><div className="mt-5 grid gap-2">{loading ? <p className="rounded-xl bg-[#f5f3ed] p-4 text-sm text-[#637073]">Loading Payments…</p> : payments.length ? payments.slice(0, 10).map(payment => <button key={payment.id} onClick={() => setSelectedId(payment.id)} className={`rounded-2xl border p-4 text-left ${selected?.id === payment.id ? "border-[#2d8d62] bg-[#edf8f0]" : "border-[#172126]/8 bg-[#fbfaf7]"}`}><div className="flex items-center justify-between gap-2"><strong>{payment.reference}</strong><Badge className={tone(payment.status)}>{label(payment.status)}</Badge></div><p className="mt-2 text-sm font-semibold">{money(payment.amount, payment.currency)}</p></button>) : <p className="rounded-xl bg-[#f5f3ed] p-4 text-sm text-[#637073]">No payment records yet.</p>}</div></section><section className="rounded-3xl border border-[#172126]/8 bg-white p-6">{selected ? <><div className="flex flex-wrap items-start justify-between gap-3"><div><p className="text-xs font-bold uppercase tracking-[0.14em] text-[#2d8d62]">Protected payment record</p><h2 className="mt-1 font-display text-2xl font-bold">{selected.reference}</h2><p className="mt-2 text-sm text-[#637073]">Review the exact amount and add required evidence when this record needs your action.</p></div><Badge className={tone(selected.status)}>{label(selected.status)}</Badge></div>{["pending_payment", "rejected"].includes(selected.status) ? <><div className="mt-5 grid gap-3 sm:grid-cols-2"><button onClick={() => setMethod("alipay")} className={`rounded-2xl border p-4 font-bold ${method === "alipay" ? "border-[#1677ff] bg-[#eef6ff] text-[#1677ff]" : "border-[#172126]/10"}`}>Alipay</button><button onClick={() => setMethod("wechat_pay")} className={`rounded-2xl border p-4 font-bold ${method === "wechat_pay" ? "border-[#09bb07] bg-[#edfaef] text-[#078d06]" : "border-[#172126]/10"}`}>WeChat Pay</button></div><div className="mt-5 grid gap-4 sm:grid-cols-2"><label className="grid gap-2 text-sm font-bold">Transfer reference (optional)<input value={reference} onChange={event => setReference(event.target.value)} className={nativeInput} placeholder="Reference in your payment app" /></label><label className="grid gap-2 text-sm font-bold">Payment screenshot<span className="flex h-11 cursor-pointer items-center rounded-xl border border-[#172126]/12 bg-[#fbfaf7] px-3 text-sm"><FileUp className="mr-2 size-4" />{proofFile?.name || "Choose screenshot"}<input className="sr-only" type="file" accept="image/jpeg,image/png,image/webp" onChange={event => setProofFile(event.target.files?.[0] ?? null)} /></span></label></div><label className="mt-4 grid gap-2 text-sm font-bold">Note for review (optional)<Textarea value={note} onChange={event => setNote(event.target.value)} className="min-h-20 rounded-xl" /></label><Button onClick={() => void submitProof()} disabled={submitting || !proofFile} className="mt-5 rounded-xl bg-[#176447] font-bold">{submitting ? "Submitting…" : "Submit payment screenshot"}</Button></> : <div className={`mt-6 rounded-2xl p-5 text-sm ${selected.status === "verified" ? "bg-[#dff5ea] text-[#176447]" : "bg-[#fff7e5] text-[#805700]"}`}><strong>{selected.status === "verified" ? "Payment verified" : "Payment verifying"}</strong><p className="mt-1">{selected.status === "verified" ? "Your protected deal details are now available." : "Your evidence is awaiting review."}</p></div>}</> : <div className="rounded-2xl bg-[#f5f3ed] p-6 text-sm text-[#637073]">Select a payment record to view its protected status.</div>}</section></div></div>;
}
