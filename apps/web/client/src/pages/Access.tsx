import { Brand } from "@/components/bridgex/Brand";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { supabase } from "@/lib/supabase";
import { ArrowLeft, CheckCircle2, Eye, ShieldCheck } from "lucide-react";
import { useEffect, useState } from "react";
import { Link, useLocation } from "wouter";
import { useAuth } from "@/_core/hooks/useAuth";

export default function Access() {
  const { isAuthenticated, loading } = useAuth(); const [, setLocation] = useLocation();
  const [mode, setMode] = useState<"signin" | "signup">("signin"); const [name, setName] = useState(""); const [email, setEmail] = useState(""); const [password, setPassword] = useState(""); const [notice, setNotice] = useState(""); const [failed, setFailed] = useState(false); const [sending, setSending] = useState(false);
  useEffect(() => { if (!loading && isAuthenticated) setLocation("/onboarding"); }, [isAuthenticated, loading, setLocation]);
  const submit = async () => { setNotice(""); setFailed(false); if (!email || password.length < 8) { setFailed(true); return setNotice("Use a valid email and a password of at least 8 characters."); } setSending(true); if (mode === "signup") { const { data, error } = await supabase.auth.signUp({ email, password, options: { data: { full_name: name } } }); setSending(false); if (error) { setFailed(true); return setNotice(error.message); } if (data.session) return setNotice("Account created. Continuing to your BridgeX profile…"); return setNotice("Account created. Confirm your email once, then sign in with your password."); } const { error } = await supabase.auth.signInWithPassword({ email, password }); setSending(false); if (error) { setFailed(true); return setNotice(error.message); } setNotice("Signed in. Opening your BridgeX profile…"); };
  return <main className="grid min-h-screen bg-[#f7f5ef] lg:grid-cols-[0.9fr_1.1fr]"><section className="hidden bg-[#172126] p-10 text-[#f7f5ef] lg:flex lg:flex-col"><Brand className="[&>span:last-child]:text-[#f7f5ef]" /><div className="my-auto max-w-md"><span className="grid size-12 place-items-center rounded-2xl bg-[#2d8d62] text-white"><ShieldCheck className="size-6" /></span><h1 className="mt-7 font-display text-5xl font-bold leading-tight tracking-[-0.05em]">Protected orders start with a trusted account.</h1><p className="mt-5 text-sm leading-7 text-[#c3d0c9]">Sign in with email and password without leaving the BridgeX app.</p></div></section><section className="flex items-center justify-center px-5 py-10"><div className="w-full max-w-[440px]"><Link href="/" className="inline-flex items-center gap-2 text-sm font-bold text-[#176447]"><ArrowLeft className="size-4" />Back to BridgeX</Link><div className="mt-9 rounded-3xl border border-[#172126]/8 bg-white p-7 sm:p-8"><div className="grid size-11 place-items-center rounded-2xl bg-[#dff5ea] text-[#176447]"><ShieldCheck className="size-5" /></div><p className="mt-6 text-xs font-bold uppercase tracking-[0.16em] text-[#2d8d62]">Account access</p><h2 className="mt-2 font-display text-3xl font-bold tracking-[-0.05em]">{mode === "signin" ? "Welcome back." : "Create your account."}</h2><div className="mt-5 grid grid-cols-2 rounded-xl bg-[#f3f1eb] p-1"><button onClick={() => setMode("signin")} className={`h-9 rounded-lg text-sm font-bold ${mode === "signin" ? "bg-white shadow-sm" : "text-[#687579]"}`}>Sign in</button><button onClick={() => setMode("signup")} className={`h-9 rounded-lg text-sm font-bold ${mode === "signup" ? "bg-white shadow-sm" : "text-[#687579]"}`}>Create account</button></div>{mode === "signup" && <Input className="mt-5 h-11 rounded-xl" placeholder="Display name" value={name} onChange={e => setName(e.target.value)} />}<Input className="mt-3 h-11 rounded-xl" type="email" placeholder="Email address" value={email} onChange={e => setEmail(e.target.value)} /><Input className="mt-3 h-11 rounded-xl" type="password" minLength={8} placeholder="Password (at least 8 characters)" value={password} onChange={e => setPassword(e.target.value)} /><Button disabled={sending} onClick={submit} className="mt-3 h-11 w-full rounded-xl bg-[#172126] font-bold">{sending ? "Please wait…" : mode === "signin" ? "Sign in securely" : "Create secure account"}</Button>{notice && <p className={`mt-3 text-sm ${failed ? "text-[#a64236]" : "text-[#176447]"}`}>{notice}</p>}<Button onClick={() => setLocation("/marketplace?guest=1")} variant="outline" className="mt-4 h-11 w-full rounded-xl bg-white font-bold"><Eye className="mr-2 size-4" />Browse as guest</Button><div className="mt-5 rounded-xl bg-[#f4faf5] p-3 text-xs leading-5 text-[#526063]"><CheckCircle2 className="mr-1 inline size-3.5 text-[#2d8d62]" />Guests can only view marketplace posts and details. Posting, offers, orders, messages, uploads, verification, and wallet actions require sign-in.</div></div></div></section></main>;
}
