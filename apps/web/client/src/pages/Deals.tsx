import { useAuth } from "@/_core/hooks/useAuth";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Textarea } from "@/components/ui/textarea";
import { supabase } from "@/lib/supabase";
import { MapPin, MessageCircle, Phone, Send, ShieldCheck } from "lucide-react";
import { useEffect, useState } from "react";

type Deal = { id: string; match_type: string; sender_id: string; traveler_id: string; status: string; sender_phone: string; sender_delivery_address: string; traveler_phone: string; traveler_pickup_address: string; accepted_at: string };
type DealMessage = { id: string; sender_id: string; body: string; created_at: string };

export default function Deals() {
  const { user } = useAuth(); const [deals, setDeals] = useState<Deal[]>([]); const [selectedId, setSelectedId] = useState(""); const [messages, setMessages] = useState<DealMessage[]>([]); const [draft, setDraft] = useState(""); const [notice, setNotice] = useState(""); const [loading, setLoading] = useState(true);
  const selected = deals.find(deal => deal.id === selectedId) ?? deals[0]; const isTraveler = selected?.traveler_id === user?.id;
  const loadDeals = async () => { if (!user) return; setLoading(true); const { data, error } = await supabase.from("matches").select("id,match_type,sender_id,traveler_id,status,sender_phone,sender_delivery_address,traveler_phone,traveler_pickup_address,accepted_at").order("accepted_at", { ascending: false }).limit(100); setLoading(false); if (error) return setNotice(error.message); setDeals((data ?? []) as Deal[]); if (!selectedId && data?.[0]) setSelectedId(data[0].id); };
  const loadMessages = async (matchId: string) => { const { data, error } = await supabase.from("match_messages").select("id,sender_id,body,created_at").eq("match_id", matchId).order("created_at", { ascending: true }).limit(300); if (error) return setNotice(error.message); setMessages((data ?? []) as DealMessage[]); };
  useEffect(() => { void loadDeals(); }, [user?.id]); useEffect(() => { if (selected?.id) void loadMessages(selected.id); }, [selected?.id]);
  const send = async () => { if (!user || !selected || !draft.trim()) return; const { error } = await supabase.from("match_messages").insert({ match_id: selected.id, sender_id: user.id, body: draft.trim() }); if (error) return setNotice(error.message); setDraft(""); await loadMessages(selected.id); };
  return <div><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="text-xs font-bold uppercase tracking-[0.16em] text-[#2d8d62]">Protected matches</p><h1 className="mt-2 font-display text-4xl font-bold tracking-[-0.055em]">Deals and private chat.</h1><p className="mt-2 max-w-2xl text-sm leading-6 text-[#637073]">Exact phone and delivery details are disclosed only after an offer or interest is accepted. BridgeX administrators can review conversations for safety and incident handling.</p></div><Badge className="rounded-full bg-[#e7f4ea] text-[#176447] hover:bg-[#e7f4ea]">{deals.length} active deal{deals.length === 1 ? "" : "s"}</Badge></div>{notice && <p className="mt-5 rounded-xl bg-[#f8e8e5] px-3 py-2 text-sm font-semibold text-[#9b4b3e]">{notice}</p>}{loading ? <p className="mt-7 rounded-2xl bg-white p-6 text-sm font-semibold text-[#647174]">Loading your protected deals…</p> : !deals.length ? <div className="mt-7 rounded-3xl border border-dashed border-[#172126]/18 bg-white p-10 text-center"><ShieldCheck className="mx-auto size-8 text-[#2d8d62]" /><h2 className="mt-4 font-bold">No accepted match yet.</h2><p className="mt-2 text-sm text-[#647174]">When a post owner accepts an offer or interest, the deal, contact details, and private chat will appear here.</p></div> : <div className="mt-7 grid gap-5 xl:grid-cols-[0.38fr_0.62fr]"><aside className="grid content-start gap-3">{deals.map(deal => <button key={deal.id} onClick={() => setSelectedId(deal.id)} className={`rounded-2xl border p-4 text-left ${selected?.id === deal.id ? "border-[#2d8d62] bg-[#edf8f0]" : "border-[#172126]/8 bg-white"}`}><p className="text-xs font-bold uppercase tracking-[0.13em] text-[#2d8d62]">{deal.match_type === "offer" ? "Traveler offer" : "Carry-space interest"}</p><p className="mt-2 font-bold">Accepted {new Date(deal.accepted_at).toLocaleDateString()}</p><p className="mt-1 text-xs text-[#667477]">Status: {deal.status}</p></button>)}</aside>{selected && <section className="rounded-3xl border border-[#172126]/8 bg-white p-6"><div className="flex flex-wrap items-start justify-between gap-3"><div><p className="text-xs font-bold uppercase tracking-[0.13em] text-[#2d8d62]">{isTraveler ? "Traveler view" : "Sender view"}</p><h2 className="mt-2 text-xl font-bold">Matched delivery details</h2></div><Badge className="bg-[#e7f4ea] text-[#176447] hover:bg-[#e7f4ea]">{selected.status}</Badge></div><div className="mt-5 grid gap-3 md:grid-cols-2"><div className="rounded-2xl bg-[#f4faf5] p-4"><p className="text-xs font-bold uppercase tracking-[0.12em] text-[#537064]">Sender delivery details</p><p className="mt-3 flex items-start gap-2 text-sm font-semibold"><Phone className="mt-0.5 size-4 text-[#2d8d62]" />{selected.sender_phone}</p><p className="mt-2 flex items-start gap-2 text-sm leading-5 text-[#526063]"><MapPin className="mt-0.5 size-4 shrink-0 text-[#2d8d62]" />{selected.sender_delivery_address}</p></div><div className="rounded-2xl bg-[#f4faf5] p-4"><p className="text-xs font-bold uppercase tracking-[0.12em] text-[#537064]">Traveler pickup details</p><p className="mt-3 flex items-start gap-2 text-sm font-semibold"><Phone className="mt-0.5 size-4 text-[#2d8d62]" />{selected.traveler_phone}</p><p className="mt-2 flex items-start gap-2 text-sm leading-5 text-[#526063]"><MapPin className="mt-0.5 size-4 shrink-0 text-[#2d8d62]" />{selected.traveler_pickup_address}</p></div></div><div className="mt-6 border-t border-[#172126]/8 pt-5"><div className="flex items-center gap-2"><MessageCircle className="size-4 text-[#2d8d62]" /><h3 className="font-bold">Private deal chat</h3></div><div className="mt-4 max-h-80 space-y-2 overflow-y-auto rounded-2xl bg-[#f6f4ee] p-4">{messages.length ? messages.map(message => <div key={message.id} className={`max-w-[82%] rounded-2xl px-3 py-2 text-sm ${message.sender_id === user?.id ? "ml-auto bg-[#172126] text-white" : "bg-white text-[#172126]"}`}><p>{message.body}</p><p className={`mt-1 text-[10px] ${message.sender_id === user?.id ? "text-[#c3d0c9]" : "text-[#738083]"}`}>{new Date(message.created_at).toLocaleString()}</p></div>) : <p className="text-sm text-[#677477]">No messages yet. Use this chat only for the accepted deal.</p>}</div><div className="mt-3 flex gap-2"><Textarea value={draft} onChange={event => setDraft(event.target.value)} placeholder="Write a message about this accepted deal…" className="min-h-11 flex-1 rounded-xl" /><Button onClick={() => void send()} disabled={!draft.trim()} className="h-11 rounded-xl bg-[#172126]"><Send className="size-4" /></Button></div></div></section>}</div>}</div>;
}
